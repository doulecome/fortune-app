# 2026-08-14

## 算命 Web App（fortune.html）：中外都优化 + 文案风格调整（跨 8/13 晚~8/14）

用户「继续打磨内容，中外都优化」+「继续往下挖，文案风格可以怎么调整」。两轮连续打磨：

### 一、西（塔罗）核心短板补齐：多牌阵位置专属融合解读
- **痛点**：三牌阵/凯尔特十字/爱情/事业/年运等多牌阵，每个位置（现状·挑战·过去·未来·建议）只贴同一段标准牌义，位置含义未融入解读。
- **新增 `posFuse(posName, t, rev)`**（app.js ~1512）：6 类位置语义（回溯/当下/前瞻/张力/行动/外部）×每类 3 变体，按 `位置|牌名|正逆` 哈希选词，生成「位置 × 牌意」专属句。同盘稳定、异盘不同。
- 注入 `runSpread`（多牌阵通用渲染）与三牌阵渲染：白话段后、指引段前输出，`.pos-fuse` 样式块。
- 验证：jsdom 10/10 断言（posFuse 位置专属句、三牌阵/凯尔特端到端、异盘不同）、ERRORS=0。

### 二、中（轻模块）去模板 + 真参数融合
- **称骨**：`_g1.._g4`/`_l1.._l4` 由 2 变体扩 3 变体，`_cgt=_hashStr('cg'+total)%3`。
- **姓名五格**：`_gUp/_gMid/_gDn` 各 2→3 变体，`_nh%3`。
- **生命灵数**：新增 `_lpPyFuse(num, py)`——依「生命灵数 × 流年数」关系给互动句（同数→同频共振 / 小→大→向上台阶 / 大→小→互补对照），注入结果段。

### 三、文案风格统一：六问判词转「国风老友」口语体
- **诊断**：全站混用三套语调——六问是「报告体」（每问必"你日主X·命局Y"+ 结尾机械重复"喜用X之气"）、塔罗是「诗意比喻体」、紫微是「老友闲聊体」（最有味但仅 2 变体、未融合喜忌）。
- **改写**：六问 SCENE 五问（career/love/study/health/wealth）的 `_v` 全部改写：
  - 错开起头：career「说事业」/ love「感情这事」/ study「读书这回事」/ health「身态上」/ wealth「财路上」。
  - 降「喜用X之气」机械重复：自然化为「往 X 上靠 / 取 X 之气 / 亲近 X」。
  - 保持 4 变体、同盘稳定异盘不同。
- 验证：jsdom ERRORS=0、六问旧起头（「你日主」「你 <b>庚」）全消、五场景均渲染新口语体。

### 四、往下挖：紫微断语扩 3 变体 + 星宫融合
- `ZW_C` 14 颗主星断语 2→3 变体（更口语老友风），`_zwPick` 改 `_zh%v.length`。
- **新增 `_zwFuse(star, gong)`**（~3452）：以星五行 × 命宫地支五行生克关系生成专属句——同气（星宫同五行→本色发挥）/ 得生（宫生星→如鱼得水）/ 泄秀（星生宫→倾注于宫位领域）/ 受克（宫克星→外境压身需打磨）/ 制化（星克宫→主动塑外境）/ 中和。注入命宫主星白话段（`mStar` 渲染处），`星·宫` 标记。
- 紫微喜忌融合自包含（星宫关系），不需依赖八字数据。
- 验证：jsdom 紫微星宫融合句出现、ERRORS=0。

### 交付与部署
- 构建 fortune.html **1063232 B（~1038 KB）**，新代码已编入（`_zwFuse`×2 / `posFuse` / `_lpPyFuse` / `说事业` 均命中）。
- 同步 `deploy/index.html` 与 `wechat-miniprogram/fortune.html`（字节一致）。
- cloudstudio 原地 redeploy，同一地址 `verified:true`：https://0279ad797a3b478488df88c02cee5abe.gz4.agentos-app.net
- 探针 `_polish.js`/`_verify2.js` 已清理。

### 注意（沿用）
- 验证探针 hour 控件填**起始小时值**（午=11 丑=1 子=0），非 0–11 序号。
- 临时探针用 Bash `rm -f` 清理（Node fs.unlinkSync 在 safe-delete 拦截下需走 trash，trash 异常时 rm 可用）。

## 八字合婚「大运/流年同步」+ 奇门「问事时辰挑吉方（时×方双轴）」（8/14 上午）

用户：「八字合婚加大运/流年同步长线维度、奇门按问事时辰挑吉方（结合吉时+方位双轴）」。两功能已落地并部署。

### 一、奇门：抽出共享 `qimenCore(now, method)`（app.js ~3847）
- 原 `qimenBtn` 内联起局逻辑抽成模块级 `qimenCore`，返回九宫飞布数据（值符/值使/门/星/神/驿马/空亡/各宫断语）。
- 字典 `QM_GONG_SHARED`/`QM_STAR_TIP_SHARED`/`QM_SHEN_TIP_SHARED`/`MEN_MEAN_SHARED`/`STAR_MEAN_SHARED`/`SHEN_MEAN_SHARED`/`MEN_WU_SHARED`/`PAL_WU_SHARED`/`PAL_TO_DIR` 提升为模块级，供主盘与挑吉方共用（原 handler 内本地副本保留未动，零回归风险）。
- `qimenBtn` 维持原渲染不变。

### 二、奇门「问事时辰挑吉方」（时 × 方 双轴）
- 奇门面板新增 `qmHour` 时辰 select + `qmPickBtn` + `qmPickBox`（ui.html）。
- `qmPickBtn`：按所选时辰重新起局（`qimenCore(d, method)`，`d` 取当日 `2*hi+1` 时，与五鼠遁口径一致）→ 空间轴取 值符临宫/开门/生门 方位；时轴用 `_jiShiCalc`+`HH_GODS`/`HH_GOOD` 判黄道/黑道。
- 输出：问事时辰黄历判定、吉方（值符/开门/生门方位+门星神）、双轴综合建议、今日黄道吉时列表。
- 验证：午时→北方(1宫·开门)、子时→东北(8宫·生门)，方轴随时辰变化 ✓。

### 三、八字合婚「大运/流年同步」长线维度
- `bz()` 增 gender 参数，算双方 `yunArr`（起运岁数+各步干支+对应公历年份 cStart）与 `qy`。
- 新增 `hehunSyncHTML(m,f)`：20 年窗口（cy-1~cy+18），每年算 大运共振（双方运干生扶日主为吉/克泄为逆）+ 流年互动（天干五合、生肖三合/六合/冲），生成彩色时间轴（绿=双顺★/红=冲煞/黄=单顺/灰=平）、双人运共振窗口、流年宜婚/宜守之年、双版结语。
- 注入合婚结果（合婚细节块之后）。

### 交付与部署
- 构建 fortune.html **924 KB**（原 910 KB）。node --check 通过；jsdom 探针断言 qimenBoard/qimenCore/qmPickBox/hehunSync 全过、ERRORS=0（canvas getContext 为 jsdom 限制，已过滤）。
- 同步 `deploy/index.html` + `wechat-miniprogram/fortune.html`，原地 redeploy `verified:true`。探针已清理。
- 注意：jsdom 在 Git Bash 下用 `/c/` 路径会被 Node 解析成 `C:\c\...` 导致模块找不到；require jsdom 须用 Windows 绝对路径 `C:\Users\42134\.workbuddy\binaries\node\workspace\node_modules\jsdom`。
- 上一轮（8/13 23:56）记忆已完整落盘，本轮无遗漏。

## 算命 Web App：紫微十二宫逐宫断语 + 塔罗大阿卡那「当下处境」（追加）

用户「紫微十二宫逐宫断语深挖、塔罗大阿卡那逐张加「当下处境」专属句」。两处内容深挖：

### 一、紫微十二宫逐宫断语
- **痛点**：紫微十二宫盘面（zw-grid）每宫只有星名/旺衰/大限/流年标签，无断语；只有命宫有 `_zwPick`+`_zwFuse`。
- **新增**（STAR_WU 之后、rows 之前插入，自包含不依赖 ZW_C）：
  - `STAR_TAG`：14 主星 → 6 大领域标签（贵=紫微/太阳、财=武曲/天府、谋=天机/巨门、动=七杀/破军、福=天同/天梁/天相、才情=廉贞/贪狼/太阴）。
  - `ZW_GONG`：11 宫（命宫除外，命宫用 _zwPick/_zwFuse）×6 类 ×2 变体 = 132 句，◇=星名占位，渲染时替换。
  - `_zwGong(gong, ss)`：取该宫第一颗主星（清洗四化括号 `(禄)` 等）→ 查标签 → 查宫位断语池 → `_hashStr(ms+'|'+gong)` 选变体。命宫返回空。
- **注入**：rows 每宫 `<div class="zw-gong-say">`（zw-stars 之后），样式加在 ui.html `.zw-stars` 后（9.5px 灰金、左上虚线分隔、左对齐）。
- 验证：jsdom 12 宫断语出现、命宫无 gong-say、异盘（1990-06-15 男 vs 1985-02-03 女）断语不同、ERRORS=0。

### 二、塔罗大阿卡那「当下处境」专属句
- **新增 `TAROT_NOW`**（TAROT 数组之后，独立字典不改原数组）：22 张大牌（0愚者~20世界）每张 now（正位处境）+ nowR（逆位处境）。
- **注入 `tarotReadingHTML`**（1606）：白话解读段后、指引段前加「当下处境」段（翡翠绿 #5a8a6a 左边框，区别于白话=朱砂、指引=金）。小牌无 TAROT_NOW 键→不渲染。
- 验证：愚者正/逆位均现「当下处境」且内容不同、小牌无、ERRORS=0。

### 交付
- fortune.html **875 KB**（app 段 292461 B），ZW_GONG/TAROT_NOW/当下处境/zw-gong-say 均编入。
- 同步 deploy/index.html 与 wechat-miniprogram/fortune.html（1076255 字节一致）。
- 探针 `_verify3.js` 已清理。

### 探针要点（沿用）
- 顶层 `const` 不挂 window 属性 → jsdom 探针访问全局常量用 `window.eval('TAROT_NOW')`。
- 紫微按钮字段：`zvBirth`/`zvHour`(起始小时值)/`zvGender`/`ziweiSchool`。

## 算命 Web App：奇门九宫逐宫断语 + 紫微辅星断语 + 塔罗小牌「当下处境」（追加）

用户三连：奇门九宫逐宫断语（对标紫微十二宫）、紫微辅星参与断语、塔罗小牌也补「当下处境」。

### 一、奇门九宫逐宫断语
- **新增**（ORDER 之后、cells 前）：`QM_GONG`（9 宫×门吉/凶/平×2 变体=54 句，含洛书方位与领域）+ `QM_STAR_TIP`（天芮/天辅/天英/天蓬/天柱/天冲点缀）+ `QM_SHEN_TIP`（蛇/虎/玄/合/天/地点缀）+ `_qmGongSay(pal,men,star,shen)`：门吉（休/生/开）/凶（死/惊/伤）/平 → 哈希选句，特殊星神再追加。
- **注入** cells 渲染：`<div class="qm-say">`（神之后），ui.html `.qmcell .sub` 后加 `.qm-say` 样式（10.5px 灰金、左对齐）。
- 验证：9 宫断语全现、实质内容、ERRORS=0。

### 二、紫微辅星参与断语
- **新增**（_zwGong 后）：`ZW_FU`（左辅/右弼/文昌/文曲/禄存 ×2 变体，{宫} 占位）+ `_zwFuSay(stars,gong)`：清洗四化括号后查该宫辅星，逐星生成句。
- **注入** rows 渲染：zw-gong-say 后加 `<div class="zw-fu-say">`（命宫亦有），ui.html 加 `.zw-fu-say` 样式（金色系 #d9a86a 区别于主星断语灰金）。
- 验证：1990-06-15 男盘出现 5 条辅星句（文昌入兄弟/右弼入子女等）、异盘不同、ERRORS=0。

### 三、塔罗小牌「当下处境」（56 张全覆盖）
- **新增 `minorNowHTML(t,rev)`**（TAROT_NOW 后）：规则生成去模板——
  - 花色领域：权杖（火·行动）/圣杯（水·情感）/宝剑（风·思维）/星币（土·物质）×2 变体（哈希选）；
  - 数字阶段：Ace（萌芽）/2-3（起步）/4-6（磨合）/7-10（关键）正逆各异；
  - 宫廷角色：侍从（学习）/骑士（追逐）/王后（滋养）/国王（掌控）正逆各异。
- **注入** tarotReadingHTML：`TAROT_NOW[t.n]||/^(权杖|圣杯|宝剑|星币)/` 双条件，大牌走字典、小牌走生成器，同一「当下处境」段。
- 验证：权杖 Ace 正逆出现且不同、数字/宫廷小牌抽查全含、大牌仍生效、ERRORS=0。

### 交付
- fortune.html **880 KB**（app 段 297267 B），QM_GONG/ZW_FU/minorNowHTML/qm-say/zw-fu-say 均编入。
- 同步 deploy/index.html 与 wechat-miniprogram/fortune.html（1086115 字节一致）。
- 探针 `_verify4.js` 已清理。

### 新增探针要点
- 奇门字段：`qmScene`（场景）+ `qimenMethod`（流派）；奇门用**当前时间**起局（无生辰输入），异局验证需跨时刻，探针内只验内容出现。
- 塔罗小牌处境为规则生成（花色×数字/宫廷），无需逐张手写。

## 算命 Web App：六爻/梅花逐位断语 + 奇门时家问事场景深挖（追加）

用户「六爻/梅花卦象也补逐位断语、奇门「时家问事」按场景深挖」。

### 一、六爻逐爻位断语（初爻至上爻）
- **新增**（liuyaoSceneLine 前）：`YAO_POS_S`（6 位名×2 变体：初爻根基/二爻近身/三爻门户/四爻外援/五爻尊贵/上爻结局）+ `YAO_QIN_TIP`（六亲提示×3）+ `_yaoLineSay(i,qin,dong,hexName)`：位名×六亲×动/静哈希选句。
- **注入**：liuyaoBtn 渲染「六亲发动」后加「逐爻细看」段（6 行，逐爻带 动/静 标记）。

### 二、梅花动爻位次断语
- **新增**（meihuaSceneLine 前）：`MH_DONG_POS`（动爻 1-6 位×2 变体：初爻根基/二爻自身/三爻门户/四爻外援/五爻核心/上爻结局）。
- **注入**：meihuaBtn 渲染「第 N 爻动」下加位次专属句（金色）。

### 三、奇门「时家问事」场景深挖
- **新增 `QM_SCENE_TXT`**（qimenSceneLine 前）：9 场景（事业/求财/感情/出行/健康/学业/诉讼/寻物/决策）×（tip 用神点评 + sp 时空 + act 行动）× 2 变体 = **54 句**；`{门}{星}{神}{sh}{ma}{kong}` 占位，kong 转宫名（坎/艮…）。
- **重构 qimenSceneLine**：保留原 USE/PO/base/extra，末尾加「深断」段（翡翠绿左边框，tip+sp+act 三行，哈希选变体+变量替换）。

### 验证与交付
- jsdom：六爻「逐爻细看」6 位齐+动/静标记、梅花动爻位次句、奇门 9 场景全出深断且无残占位，ERRORS=0。
- **探针要点**：六爻/梅花是投掷动画流程（~1.3s 计时器后才写结果）→ 探针须在点击后 setTimeout 3000ms 再断言。
- fortune.html **886 KB**（app 段 302863 B），已同步 deploy/wechat-miniprogram，原地 redeploy 同一地址。

至此：八字/紫微/奇门/六爻/梅花/塔罗全部有逐位/逐模块专属断语。剩余可选：六爻六神逐位深化、塔罗小牌数字逐张手写、原生小程序 MVP。

## 算命 Web App：六爻六神逐位深化 + 塔罗小牌 56 张逐张手写处境句（追加）

用户「六爻六神（青龙/朱雀/勾陈…）逐位深化、塔罗小牌 56 张逐张手写处境句」。

### 一、六爻六神逐位深化
- **新增**（_yaoLineSay 后）：`LIU_SHEN_MEAN`（青龙主喜/朱雀主口舌/勾陈主迟滞/腾蛇主虚惊/白虎主凶伤/玄武主盗失 ×2 变体）+ `_shenSay(s,seed)` 哈希选句。
- **注入**：六爻「逐爻细看」每行加第二行 `六神·X：` 专属白话（shen[i%6]，shen=sixShen(getDayGanNow()) 按日干定起位）。
- 验证：逐爻细看 6 神全现（白虎/青龙等实测全出）、ERRORS=0。

### 二、塔罗小牌 56 张逐张手写处境句
- **新增 `MINOR_NOW`**：56 张小牌逐张手写 now/nowR（112 句，按韦特牌意扩写为"此时此地"），权杖/圣杯/宝剑/星币 × Ace~10/侍从/骑士/王后/国王。
- **tarotReadingHTML 字典优先**：`TAROT_NOW[t.n] ? 大牌 : (MINOR_NOW[t.n] ? 小牌手写 : minorNowHTML 兜底)`——手写句覆盖全部 56 张，规则生成仅兜底。
- 验证：56 张全覆盖、正逆均非空且不同、权杖10/星币王后均用字典句、ERRORS=0。

### 交付
- fortune.html **892 KB**（app 段 308875 B），LIU_SHEN_MEAN/MINOR_NOW/六神· 均编入。
- 同步 deploy/index.html 与 wechat-miniprogram/fortune.html（1111710 字节一致），原地 redeploy 同一地址。
- 探针 `_verify6.js` 已清理。

### 至此内容覆盖总览
八字（六问/流月/神煞/大运流年）· 紫微（十二宫+辅星+星宫融合）· 奇门（九宫+时家问事9场景深断）· 六爻（逐爻+六神+世应持世）· 梅花（体用+动爻位次）· 塔罗（78 张全量当下处境，大牌字典+小牌手写）· 称骨/姓名/灵数（3 变体）全部去模板带专属断语。

## 算命 Web App：运势类模块（择日/梦境）再扩内容（追加）

用户「运势类模块（择日/梦境）再扩内容」。

### 一、周公解梦 DREAMS 扩 16 条（30→46）
新增：鱼（余财）/厕所（排解）/地震（惊变）/洪水（财情大起落）/蜘蛛（巧思人脉）/老鼠（小耗）/钥匙（解结）/门（路径）/镜子（自照）/刀剪（决断）/鬼怪（心魔）/裸体（坦诚）/迟到（失机）/星星（吉兆）/月亮（圆满）/钱包（资财）。
- 验证：8 个新关键词（鱼/厕所/地震/鬼/钱包/大雨/迟到/满月）全部命中。

### 二、梦境卡 DREAMO 扩 20 条（22→42）
追加诗意灵感句 20 条（木门/海梯/影子/云朵羊群/旧收音机/图书馆/霜花/萤火虫/水底挥手/会唱歌的种子/停电夜/终点花开/树上名字/糖霜雪/纸鹤晚安/鲸鱼哼歌/发光的零钱/猫开会/无墙房间/替我说出口）。
- 注：**DREAMO 原实际仅 22 条**（非记忆中 30），22+12+8=42。

### 三、择日扩：常用事项 chips + 吉日宜忌明细
- `PICK_CHIPS`（嫁娶/开业/搬家/出行/动土/签约/求医/入学/置业/祈福/求财）+ `window.pickFill(t)`（填充 select 并触发）。
- 结果加 chips 行 + 「吉日宜忌」明细（前 3 吉日的 宜/忌 各列 8 项，lunar getDayYi/getDayJi）。
- **关键坑（记住）**：`pickThing` 是 `<select>`（ui.html 1742，有效 value=嫁娶/开业/搬家/出行/动土/签约/求医/入学/置业/祈福/求财），chips 值必须对齐 option value——之前用「结婚/入宅/考试」等无效值导致 select.value 被清空、择日 thing 变空。

### 交付
- fortune.html **894 KB**（app 段 311345 B），同步 deploy/wechat-miniprogram（1116778 字节一致），原地 redeploy。
- 探针 `_verify7.js` 已清理。

## 算命 Web App：黄历主面板加深 + 今日运势聚合卡扩内容（追加）

用户「黄历主面板（宜忌页）再加深（每日吉时/喜神方位明细）、今日运势聚合卡扩内容」。

### 一、黄黑道吉时自算 `_jiShiCalc(dayZhi)`（库无吉时接口）
- 规则：日支定起神（子午青龙/丑未明堂/寅申天刑/卯酉朱雀/辰戌金匮/巳亥天德 = 日支索引%6），十二时辰顺排 12 神（青龙/明堂/天刑/朱雀/金匮/天德/白虎/玉堂/天牢/玄武/司命/勾陈）；黄道六神（青龙/明堂/金匮/天德/玉堂/司命）所临时辰为吉时（每天 6 个）。
- 实测 2026-08-14（庚申日）：吉时 寅卯巳申戌亥时。

### 二、黄历主面板加深（alBtn）
- 喜财福神升级：卦名+方位 Desc（`getDayPositionXiDesc` 返回「西北」纯方位，`getDayPositionXi` 返回卦名「乾」）+ 用事建议（喜宜求喜/约会/宴客，财宜求财/签约/讨账，福宜祈福/安宅/开市）。
- 新增行：吉时（黄黑道）+ 六曜（`getLiuYao`）+ 旬空（`getDayXunKong`）+ 纳音（`getDayNaYin`）。

### 三、今日运势聚合卡扩（todayBtn）
- pills 加：纳音/六曜/旬空/冲煞。
- 新增块：吉时行、神方位明细行、**星座寄语 XZ_TIPS（12 星座×3 变体，按日期轮换）**，生肖寄语并列。
- 尾注更新为干支/宜忌/幸运/生肖/星座/吉时/神方位均真实推算。

### 验证
- 13/13 断言、ERRORS=0（黄道规则自洽：子日 6 吉时含子时、子午同、丑日偏移不同；黄历/聚合新内容全现）。
- 探针教训：丑日明堂起于子时→子时也是吉时，断言「丑日不含子时」是错的，应断言「列表≠子日」。

### 交付
- fortune.html **897 KB**（app 段 314233 B），同步 deploy/wechat-miniprogram（1121149 字节一致），原地 redeploy。
- 探针 `_verify8.js` 已清理。

## 算命 Web App：首屏今日签/寄语扩池 + 择日「每日吉时」联动（追加）

用户「首屏今日签/寄语再扩文案池、择日模块加「每日吉时」联动（选了吉日再看吉时）」。

### 一、首屏今日签加签诗
- `SLIP_POEMS` 32 句签诗池（按日干支 `_hashStr(gz)` 选，同日稳定异日不同），渲染 `签诗 · X` 行（slip-poem）。
- 今日签结构升级：干支+农历 → 签诗 → 宜忌 → 五行。
- 实测 2026-08-14（庚申）：签诗「晨光熹微，宜行宜止，随心而动」。

### 二、首屏今日寄语扩 16 句（32→48）
新增：知者不惑/流水不争先/万物皆有裂痕/花未全开月未圆/身在井隅/凡是过往皆为序章/慢品人间烟火色/人生如逆旅/路漫漫其修远兮/世界微尘里/枕上诗书/把酒祝东风/人生得意须尽欢/采菊东篱下/此心安处是吾乡 等。

### 三、择日「每日吉时」联动
- 30 天格子全部加 `onclick="pickDayJiShi(iso)"` + cursor:pointer + title「点击看当日吉时」。
- 新增 `window.pickDayJiShi(iso)`：按 iso 起 lunar → 显示 干支/吉时（_jiShiCalc 黄黑道）/宜/忌 到 `#pickJiShiBox`。
- 「吉日宜忌」升级「吉日宜忌 · 吉时」：前 3 吉日各带吉时。
- 实测 8/14 点击：8月14日 · 庚申 · 七月初二，吉时 寅卯巳申戌亥时。

### 验证
- 10/10 断言、ERRORS=0（签诗出现、寄语新句编入、格子 onclick、吉时详情/干支显示）。
- 探针要点：`#pickJiShiBox` 只有点击 pickDayBtn 后才存在于 DOM → 测 pickDayJiShi 前必须先点按钮渲染 box。

### 交付
- fortune.html **899 KB**（app 段 316357 B），同步 deploy/wechat-miniprogram（1124747 字节一致），原地 redeploy。
- 探针 `_verify9.js` 已清理。

## 算命 Web App：姓名五格/数理扩变体 + 数字能量/生肖运势扩内容（追加）

用户「姓名五格/数理判词扩变体、数字能量/生肖运势卡扩内容」。

### 一、姓名五格判词 3→4 变体 + 数理点睛
- `_gUpS/_gUpL/_gMidS/_gMidL/_gDnS/_gDnL` 各加 1 句（4 变体），渲染 `_nh%3`→`%4`。
- 新增「数理点睛」块：按 人格+总格 吉凶组合（`shuInfo(ren)[2]`/`shuInfo(total)[2]`）三档（双吉/双凶/混合）各 2 变体，哈希选句。

### 二、数字能量加「号码解读」块
- `_readPool`：主导星（天医/延年/生气/伏位/绝命/五鬼/六煞/祸害）各 2 变体专属解读句 + 吉凶平衡句（吉多/凶多/相当 三态）。

### 三、生肖寄语 3→5 变体（12×5=60 句）+ 今日生肖幸运行
- sxTips 每生肖加 2 句（鼠肖宜快人一步/牛肖厚积薄发…）。
- 聚合卡生肖块加「幸运：{sw}系色 ｜ 数字 X ｜ 方位 Y」（按生肖地支五行 DZ_WU[sxZhi] 推导，复用 WU_COLOR/WU_DIR/luckyOf 数字表）。
- **小坑**：渲染前缀与 sxLucky 自带「幸运：」重复成「生肖幸运：幸运：」→ 渲染只用 `${sxLucky}`。

### 验证
- 6/6 断言、ERRORS=0（数理点睛块+异名判词不同、号码解读主导星句、生肖幸运行+sxTips 5 变体）。
- 探针教训：改渲染文案后探针旧断言（「生肖幸运」字样）过时，需同步更新。

### 交付
- fortune.html **907 KB**（app 段 323623 B），同步 deploy/wechat-miniprogram（1140249 字节一致），原地 redeploy。
- 探针 `_verify12.js` 已清理。

## 算命 Web App：观音灵签扩池 + 术语词典扩词 + 塔罗搜索增强（追加）

用户「灵签（观音签）再扩签池、塔罗牌面搜索/术语词典扩词条」。

### 一、观音灵签 GUANYIN 扩 12 签（28→40）
- 新增 12 签（含上上 4/中上 3/中平 3/中下 1/下下 2）：明月照寒潭/雨后青山/行船打头风/久雨不晴/玉堂金马/双燕归巢/金鳞跃波/独木桥头/寒梅傲雪/一池春水/云中鹤唳/破屋难遮。
- 实测等级分布：上上9/中上10/中平10/中下3/下下8。

### 二、术语词典 TERM_DICT 扩 17 词（40→57）
新增：黄黑道/吉时/六曜/彭祖百忌/喜神/财神/福神/冲煞/值星/大阿卡那/小阿卡那/正位/逆位/牌阵/凯尔特十字/六神/体党。
- wrapTerms 悬浮释义自动覆盖新词。

### 三、塔罗牌意搜索增强
- 匹配词金色高亮（牌名/关键字/牌义，正则转义防注入）。
- 结果顶部「共命中 N 张」计数；无结果给建议词「试试：爱情/事业/财富/逆位/权杖/月亮」。

### 验证
- 10/10 断言、ERRORS=0（签池 40+等级分布、词典 57+wrapTerms 命中、搜索高亮/计数/无结果建议）。

### 交付
- fortune.html **901 KB**（app 段 318286 B），同步 deploy/wechat-miniprogram（1129002 字节一致），原地 redeploy。
- 探针 `_verify10.js` 已清理。

## 算命 Web App：六爻/梅花扩场景 + 生肖配对/幸运色扩内容（追加）

用户「六爻/梅花再扩问事场景、幸运色/生肖配对扩内容」。

### 一、六爻/梅花各扩 6 个问事场景（9→15）
- ui.html `lyScene`/`mhScene` 各加 6 option：求职/家宅/合作/求医/孕事/讨债。
- 六爻 USE：求职官鬼/家宅父母/合作兄弟·妻财/求医子孙/孕事子孙/讨债妻财·官鬼；useQin 特判加 合作=['兄弟','妻财']、讨债=['妻财','官鬼']。
- 梅花 USE：求职职位/家宅宅基/合作合伙/求医药石/孕事子息/讨债所欠。
- 验证：六爻「问求职·官鬼」、梅花「问合作·合伙」判词出现。

### 二、生肖配对扩内容
- 新增 `ZOD_TRAIT`（12 生肖各一句特质）+ 渲染「特质呼应」块（两生肖特质并列）。
- `ZT_TIP` 每类 2→3 变体（7 类×3=21 句）。
- 相处建议 2 档→4 档（≥85 上等缘分/≥70 良好/≥50 中等/<50 冲克）+ 关系类型后缀（六冲六害相刑→求同存异功课、六合三合→顺水推舟）。

### 三、幸运色扩五行宜忌
- 新增 `WU_DAILY`（五行×ok宜/no忌/wear穿着/food饮食/tip贴士），luckyResult 加「五行宜忌 · X」块 + 贴士行。
- 例（金日）：宜决策签约向西方，忌优柔寡断过度消费，穿白金银色，食清淡润肺，贴士「金主收敛，宜断舍离」。

### 验证
- 8/8 断言、ERRORS=0（六爻求职/梅花合作判词、生肖 select 12 项+特质呼应+鼠牛六合+建议细化、幸运色五行宜忌+穿衣饮食）。

### 交付
- fortune.html **904 KB**（app 段 320759 B），同步 deploy/wechat-miniprogram（1134450 字节一致），原地 redeploy。
- 探针 `_verify11.js` 已清理。

## 算命 Web App：合婚深化（星座配对+细节块）+ 奇门九宫方位速查（追加）

用户「星座配对/合盘再深化、奇门九宫按「问事方位」出建议」。

### 一、合婚深化（hehunBtn）
- `bz()` 扩展返回 `xz`（solar.getXingZuo）。
- 新增星座元素相性行：男/女星座 + 火土风水元素 + EL_REL 16 组合相性句（火借风势/水土相润/火水相激…）。
- 新增「合婚细节」块（翡翠绿左边框）：
  - 日干五行生克方向（男方X生/克女方Y，含主导方建议）；
  - 生肖关系细化（复用 zodiacRel → 六合/三合/六冲/六害/相刑/同属/普通 + 分数 + tip）；
  - 综合结语 2 变体哈希（综合可处之缘/总体看）。
- 注意：zodiacRel 定义在合婚代码之后（4113），但 function 声明提升 + onclick 回调执行时 ZOD 已定义，可安全调用。

### 二、奇门九宫按「问事方位」出建议
- 全局 `QM_DIR_MAP`（东3/南9/西7/北1/东南4/西南2/东北8/西北6/中5）+ `QM_DIR_USE`（9 方位用事建议）。
- qimenBtn 渲染 qmtable 后加「问事方位速查」9 方位 chips + `#qmDirBox` 详情区。
- `window.qmDirPick(dir)`：查 `window._qmDirData`（onclick 内每宫 sayByPal/menB/starB/shenB/zhiFuPal 快照——**闭包变量不能跨函数访问，必须存 window**）→ 显示 方位·宫（洛书名）+ 门/星/神 + _qmGongSay 断语 + 「此方宜」用事建议 + 值符所临标记。
- 实测（今局）：东方·3宫（震）门景星禽神虎，「门平可动可静；白虎临宫防刑伤宜避其锋；此方宜行动开创」。

### 验证
- 8/8 断言、ERRORS=0（合婚细节块+星座元素行+日干五行方向；奇门方位 chips+qmDirPick 东方/北方内容各异）。

### 交付
- fortune.html **910 KB**（app 段 327015 B），同步 deploy/wechat-miniprogram（1144995 字节一致），原地 redeploy。
- 探针 `_verify13.js` 已清理。

## 算命 Web App：文案去重深化 + 奇门谈判/签约场景 + 合婚大运图增强（「124」轮）

用户「124」= 一次做三件事：①文案去重深化（紫微/六爻/梅花）＋②奇门谈判/签约场景＋④合婚大运图增强（hover 详情 + 导出 PNG）。

### 一、文案去重深化（①）
- `ZW_FU`（3678）：左辅/右弼/文昌/文曲/禄存 各加第 3 句（仍用 `{宫}` 占位）→ 池 2→3 变体。
- `LIU_SHEN_MEAN`（3342）：青龙/朱雀/勾陈/腾蛇/白虎/玄武 各加第 3 句。
- `MH_DONG_POS`（3465）：6 位各加第 3 句动爻位次断语；调用 `%MH_DONG_POS[dong-1].length`（原 `%2` 修正，适配新长度）。
- 三处调用均 `_hashStr(...)%arr.length`，同盘稳定、异盘不同，自动降雷同。

### 二、奇门新增谈判 / 签约场景（②）
- `QM_SCENE_TXT` 在 `决策` 后新增 谈判/签约（每场景 tip/sp/act 各 2 变体，含 `{门}{星}{神}{sh}{ma}{kong}` 占位）。
- `qimenSceneLine` USE/PO 扩：谈判=六合（博弈）/签约=开门（契约）；减力=议和减力/契约减力。
- extra 分支：谈判 六合/九地/九天临宫差异；签约 值符/天芮/玄武临宫差异（防条款陷阱）。
- `ui.html` qmScene select 在 `决策` 后加 `<option value="谈判">` + `<option value="签约">`。

### 三、合婚大运图增强（④）
- `hehunSyncHTML` 时间轴每格由 `title=` 改 `class="htl-cell" data-tip="..."`（hover 详情：年/双方大运干支+纳音/流年标签/互动）。
- 写 `window.__hehunTL={rows,cy,bestWin,jiYears,badYears,mName,fName}`，图例行加「悬停每格看当年明细」，块尾加导出按钮 `onclick="hehunExportImg()"`。
- 新增全局 hover tooltip（mouseover/mousemove/mouseout 委托 `.htl-cell`）+ `hehunExportImg()`：拼 SVG（两行 20 年、优★ 绿色/凶 红/中 黄、图例+共振窗口+宜婚/宜守）→ Image.onload → canvas.toDataURL → 下载 PNG。

### 四、构建 / 部署防回归
- `build.js` 在 `writeFileSync(fortune.html)` 后加 `writeFileSync(index.html,out)`：保持部署入口 `index.html` 同步（CloudStudio 以根 `index.html` 为入口）。
- 新建 `index.html`（930 KB）根副本。

### 验证
- 探针断言 A1=长线维度出现/A2=导出按钮/A3=`__hehunTL` 有 rows/A4=`htl-cell` 单元格/B1=谈判场景/B1b=签约场景/B2=下拉含两项/C1=`hehunExportImg` 为函数 → ALL_PASS=true（errs 空）。
- `node --check` 通过；fortune.html / index.html 均 1173121 B；deploy/wechat-miniprogram 三端一致。
- 部署：新沙箱 `59299e91e4e74600b4020b8c01d9f5b1`，verified:true（首次因缺 index.html 才 false，补后即复用同地址）。

### 交付
- 线上地址：`https://59299e91e4e74600b4020b8c01d9f5b1.app.workbuddy.link`
- fortune.html / index.html **930 KB**（app 段 346382 B），同步 deploy/wechat-miniprogram，已部署。

## 算命 Web App：五功能深化（用户贴表「可继续点」全做）

用户把能力全景表的「可继续点」列当工作单：①奇门补场景 ②文案去重扩3变体 ③合婚加雷达 ④生肖/星座加年度共振 ⑤幸运色接日主喜用。

### 一、奇门新增「求职」场景（考试≈学业/疾病≈健康/诉讼已存在，仅求职真正缺失）
- `QM_SCENE_TXT` 在 事业 后加 求职（tip/sp/act 各 2 变体，{门}{星}{神}{sh}{ma}{kong} 占位）；`qimenSceneLine` USE/PO 加 求职=开门（机遇）/求职机遇减力；extra 分支加 求职（值符临宫正途直投、驿马多跑动、玄武防招聘陷阱）；`ui.html` qmScene 下拉加「求职 / 应聘」。

### 二、文案去重扩 3 变体（八字日柱 + 奇门九星）
- `GAN_C`（baziRead 内）由单句改 3 变体数组；②日主行 `GAN_C[dayGan]` 改 `_pick(GAN_C[dayGan],'gan')` 哈希选（同盘稳、异盘异）。
- `STAR_MEAN_SHARED` / `STAR_MEAN`（九星）改 3 变体数组；4157 行 `${STAR_MEAN[zfStar]}` 改哈希选；`QM_STAR_TIP_SHARED` 改 3 变体，3999 行 `st[0]` 改哈希选。
- 紫微主星 `ZW_C` 此前已 3 变体，跳过（注释仍写「2 变体」属陈旧，未动）。

### 三、合婚加「五行互补 · 用神雷达」
- `bz()` 扩展返回 `wuCnt`（四柱干支五行计数：ec.getYear/Month/Day/HourGanZhi + GAN_WU/DZ_WU）。
- 新增 `wuComplementRadar(m,f)`：五边形 SVG（金木水火土）双色叠加（男青/女玫瑰）+ 互补度＝100−余弦相似度，差异越大越互补；渲染进合婚结果块。

### 四、生肖/星座「年度运势共振」
- 新增 `yearResonanceHTML(i,j,xzM,xzF)`：当年太岁（getYearInGanZhi 年支）→ 复用 `zodiacRel(生肖idx, 太岁idx)` 判定值/冲/刑/害/合/三合；星座元素 vs 流年五行（SHENG/KE）给星座年运；共振结论（双星同顺/同守/一顺一守/平年）。
- 合婚结果块 + 生肖配对 zodiacBtn 结果块均接入（合婚带星座参数、生肖配对不带）。

### 五、幸运色接日主喜用做个性化
- `ui.html` 幸运色面板加「生日（可选）」`#luckyBirth` 输入。
- 新增 `luckyPersonalize(birth)`：日主五行 + 扶抑近似喜用（生扶日主＝日主同类+印星）；`luckyBtn` 有生日时加「本命视角」块（喜用色+宜忌），与当日流日色叠加提示。

### 构建 / 同步 / 验证 / 部署
- `node --check` app.js & build.js 通过；build.js 生成 fortune.html+index.html（app 段 355876 B，939 KB / 1187852 B）；手动 `cp` 同步 deploy/index.html + wechat-miniprogram/fortune.html（三端 1187852 B 一致）。
- 探针 `_verify16.js`（jsdom）断言 A求职字典/B求职判词/C九星3变体/D九星小注3变体/E幸运色个性化/F合婚雷达+年度共振/G生肖配对共振/H生日输入/I下拉项 → ALL_PASS=true（errs 空）。注意：const 全局不经 window 暴露，须用 `window.eval` 在全局作用域读取。
- 部署：复用沙箱 `59299e91e4e74600b4020b8c01d9f5b1`，verified:true。探针已清理。

### 交付
- 线上地址不变：`https://59299e91e4e74600b4020b8c01d9f5b1.app.workbuddy.link`
- fortune.html / index.html **939 KB**（app 段 355876 B），同步 deploy/wechat-miniprogram，已部署。
- 任务 #216-#219 全部 completed；探针 `_verify15.js` 已清理。
