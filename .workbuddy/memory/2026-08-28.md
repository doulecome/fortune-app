# 2026-08-28 工作日志

## 诊断：主源 D:/deploy/index.html 被覆盖退化（03:05）

- 现象：`D:/WorkBuddy/2026-08-05-00-54-43/deploy/index.html` 于 08-28 03:05 被改动，md5 由 `8a7d0a7c`（08-26 23:59 同步的完整版）变为 `56637738`，体积 2098566 → 2094818（少 3748 字节）。
- diff 结论：**纯删除，非功能改动**。改的全在 `<style>` 区，JS 功能完好（8 项功能 grep 全在、26 tab 全在、node --check 通过、标签配对正常）。
- 丢失清单（均为动效/视觉增强）：
  1. `body.theme-dark` 暗色主题整块 + 暗色 .btn 渐变（2 处）
  2. `.panel.show` 卷轴展开 `scrollUnfurl` 动画（2 处）
  3. `@keyframes ripple` / `fxpop` / `bloomCore`
  4. `.card::after` 跟随鼠标高光 + hover 光晕（桌面 + 移动端 2 处）
  5. 卡片右上朱砂角（`.card::after` 三角）
  6. 五行 chips 古典配色（5 条 `!important`）
  7. `h1Open` 主标题「墨落笔意」落笔动画
  8. `.btn:active` 按压反馈、`.tab,.cat` transition
  9. 宣纸纹理（2 层 radial-gradient + 噪点）→ 被替换为 斜纹 repeating-linear-gradient + 噪点（4 层，size 160px）
- 影响面：**线上未受影响**——C 盘部署端 deploy/index.html 仍是 `8a7d0a7c`；D 盘根目录 index.html / fortune.html / wechat-miniprogram/index.html 也都是完整版。仅主源退化。
- 已做：备份退化版 → `deploy/index.html.bak-20260828-0305`（md5 56637738）；保留 diff → `.diff_new.txt`。
- 待用户决定：是否用完整版（8a7d0a7c）覆盖主源恢复。

## 关键查证结论（决定是否恢复的依据）

- **`body.theme-dark` 不是动画，是塔罗功能**：`index.html:11441` 由 `isTarot` 触发 `classList.toggle('theme-dark')`，122 处样式全部服务塔罗紫色玻璃暗色主题（`.read-modal.style-tarot`）。删掉 = 塔罗阅读页直接变白底，功能半废。
- **五行古典色不是动画，是专业性**：`:root` 定义 `--huaging #2e5a88 / --shilv #5f8a6b / --tengHuang #d9a441 / --zheshi #8b5a2b / --yanzhi #a63d40`，5 条 `.wxchip span:nth-child(n)` 覆盖。命理产品用传统色属正确性要求。
- **丢失动画全部真实调用，非死代码**：`fxpop`（.fx-pop 抽签类元素）、`bloomCore`（.ink-bloom .bloom-core 墨晕）、`ripple`（按钮水波）、`scrollUnfurl`（.panel.show 切面板，2 处规则）。
- **已有无障碍兜底**：`prefers-reduced-motion: reduce` 共 31 处媒体查询，系统级关动效即全停。→ 动画无需为性能砍，用户可自行降级。
- 结论：03:05 那次改动丢的东西里约 2/3 是功能/配色，仅 1/3 是纯装饰动画；且动画部分有真实调用 + reduced-motion 兜底。**建议全量恢复完整版**。
- 线上核对（curl 下载线上页）：scrollUnfurl 3 处 / theme-dark 122 处 / h1Open 3 处 / bloomCore 4 处 / 朱砂角 1 处 / 评级卡 / 三档 9 处 / 透镜 2 处 → **线上确认是完整版，未受主源退化影响**。

## 已执行：恢复 + 建 git 仓库 + 推送 GitHub

- 主源恢复：用 `wechat-miniprogram/index.html`（8a7d0a7c）覆盖 `deploy/index.html`，再同步 5 端（根目录 index/fortune、小程序、C 盘部署端 index/fortune）→ 五端 md5 全部 `8a7d0a7c`。动效核对：scrollUnfurl 3 / theme-dark 122 / h1Open 3 / 朱砂角 1 / tengHuang 3 全部回归。
- 建库：`D:\WorkBuddy\2026-08-05-00-54-43` 下 `git init`，分支 `main`，提交 `0600ce4`（68 文件，.git 4.6M）。
  - 新增 `.gitignore`（排除 `.diff_new.txt`/`.remote_old*.html`/`*.bak-*`/临时校验文件）+ `.gitattributes`（`* text=auto eol=lf`，png 等 binary）。
  - 仓库级身份：user.name doulecome / user.email doulecome@users.noreply.github.com。
- 远程：**`https://github.com/doulecome/fortune-app`（私有仓库，新建）**。账号下原有仓库仅 `doulecome/quant_ai_v2`。推送成功，远程 68 文件 14.95MB，远程 sha `0600ce4cab65` 与本地一致。
- 后续 push 必须的本地 git 配置（已写入 .git/config，**勿删**）：
  - `http.proxy = socks5h://127.0.0.1:7897`、`https.proxy = socks5h://127.0.0.1:7897`（Clash mixed 端口）
  - `http.sslBackend = schannel`、`http.version = HTTP/1.1`、`http.postBuffer = 1048576`
  - 认证：本机 GCM 已存 `doulecome` 凭据，正常交互环境下直接 `git push` 即可。

## 教训（流程改进）

- 主源单文件 >2MB、无 git 版本控制，被外部编辑器/旧副本覆盖时无告警。建议在每次写操作前先 `cp` 一份 `.bak-<时间戳>`，或给项目加轻量 git（只 track deploy/index.html）。**本条已解决：现已有 git 仓库 + GitHub 远端。**
- 同步顺序应保持：主源（D:/deploy/index.html）→ 副本（根目录 / fortune / 小程序）→ C 盘部署端；**反向覆盖会丢功能**。
- 环境注意：managed node 实际路径是 `C:/Users/42134/.workbuddy/binaries/node/versions/22.22.2-2/node.exe`（带 `-2` 后缀），旧摘要里写的 `22.22.2` 路径已失效。
