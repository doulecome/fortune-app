# 2026-08-25 工作日志

## 回到顶部去重 + 青囊借鉴三件套（md5 2f70d695，已部署 0279ad79）
- **回到顶部去重（用户指出"有同功能了"）**：发现站点已有 `#toTop`（"玄"字圆形按钮，.to-top 样式 + 37734 行 JS），我上次加的 backTop 是重复的。处理：删除 backTop 按钮+脚本；增强原 toTop——同时监听 window 滚动和 `.stage` 内部滚动（桌面 stage 内滚时原实现不生效），点击同时滚两个容器。
- **① tab 单字图标**（青囊"单字大图标"借鉴）：26 个 tab 各加 `<span class="tic">单字</span>`（金色小徽章），单字表：命/名/骨/微/奇/签/梦/卦/缘/问/字/壬/智/数/德/幸/衣/历/时/肖/塔/座/符/雷/灵/性；CSS `.tic{background:var(--gold);color:var(--paper)}`。
- **② 铜钱摇卦动画**：易经占卦点击后先显示三枚 🪙 摇晃占位（.coin-shake keyframes，0.35s 交替），1.1s 后出结果；计算预先完成只延迟展示（不阻塞）。
- **③ 页脚古籍品牌区**：.foot 里 disc 后加一行"推算依《周易》《三命通会》《子平真诠》《滴天髓》《紫微斗数全书》《增删卜易》《梅花易数》《烟波钓叟歌》等传统典籍"。
- 验证：JS 语法 OK、26 tab 26 图标、backTop 0 残留、toTop 增强、线上确认；五端同步一致。
- 经验：加"新功能"前先 grep 是否已存在（toTop 这类），避免重复造轮子。

## 仪式感动效三件套（md5 d201fdf5，已部署 0279ad79）
- **① 抽签动画**：诸葛神数新增 zgStick 🎋 签筒 + zhugeBtn 包装（shake .6s 动画 + 800ms 延迟后 zhugeDraw，36192 行）；观音灵签原有 shake(600ms) 增强为 900ms（更明显的弹签节奏）；「随机三数」保持直出（快速路径）。
- **② 翻牌动画**：**确认塔罗已有**（fan-card 3D 翻转 + .tcard.flip + sheen 扫光 + shuffle-deck 洗牌动画），未重复添加。
- **③ 推算加载态**：新增通用 loadWrap 包装器（尾部 script），bazi/ziwei/qimen 三按钮点击先出"☯ 正在排盘推演…"墨色旋转加载（.calc-spin 1.1s），450ms 后执行原 handler（同步计算、结果覆盖占位）；不侵入原 handler。
- 验证：JS 语法 OK(4 script块)、线上 zgStick/加载态/calc-spin 全在、tab 26、五端一致。

## A1/A2/A3/B1/C1 五连（md5 b1125112，已部署 0279ad79）
- **A2 易传·大象传（64卦）**：HEX 数据结构为 {name,up,down,ci(卦辞),du(今解)}，原无彖/象字段。本次为 64 卦注入 `dx`（大象传"象曰：×上×下…君子以…"，标准古籍文本）；易经占卦结果在卦辞后展示"象曰"。彖传/文言传未硬编（避免凑数，可在结果标注来源，后续如需从可靠古籍引入）。
- **A3 解卦透明化**：易经占卦结果加「推演链路 · 本卦→变卦」折叠——本卦(上下卦)、变卦/无动爻(静卦)、动爻位(第n爻)、所问。
- **A1 断语扩容去重**：新增 RAND_TIPS 池(12条) + randTip()/randTipHtml()，占卜结果追加"✦ 随机短评"——易经/观音/诸葛/六爻/梅花 5 处接入，同结果不同次有新鲜感（不破坏原文库）。
- **B1 功能九宫格**：新增 home 面板（id=home，含 homeGrid 容器，26 卡：单字大图标+名称，点卡 switchPanel）；header 加「☰ 全部」按钮打开。卡数据从 TABS 实时渲染（复用 .tic 单字）。
- **C1 PWA**：deploy/ 新增 manifest.json(玄机阁,standalone,#8a6d3b)+sw.js(缓存 index/fortune/manifest/icon, 离线兜底 index)+icon.svg(玄字金章)；index head 加 manifest/theme-color/apple-touch-icon + SW 注册内联脚本。线上已验证三文件可访问。
- 验证：JS 语法 OK(5 script块)、dx 64、randTip 7、home 3、tab 26；五端 + C盘 deploy 同步一致（含 PWA 三文件）。
- 经验：A2 这类"古籍内容"必须保准确，宁缺毋滥（零凑数）；PWA 三文件需随 deploy 目录一起同步到 C 盘旧路径才能上旧沙箱。

## 语音输入 + 占卜音效（md5 2370d606，已部署 0279ad79）
- **① 语音输入问事**：Web Speech API（zh-CN）——易经(ichingAsk)与奇门(qmAsk)输入框旁加 🎤 按钮（ichingVoice/qmVoice），点击聆听→识别→自动填入；不支持则 placeholder 提示。tail script 里 bindVoice 绑定。
- **② 占卜音效**：**发现 sfx() 系统已存在**（40771 行，guqinNote 合成，类型 incense/seal/flip/switch，受 _guiOn 琴音开关控制，tab 点击→switch、牌点击→flip 已接线）。本次补 coin(掷钱)/sign(摇签) 两型 + 接线：ichingBtn→coin、gyBtn/zhugeBtn→sign（hook 包装，不侵入原 handler）。
- 音效全受设置里琴音开关统一控制；塔罗翻牌音效原本就有。
- 验证：JS 语法 OK、麦克风 2、绑定 2、sfx 2 型、接线 3、线上确认、tab 26、五端一致。
- 经验：凡"动画/音效/按钮"类需求，先 grep 现有机制（sfx/_guiOn/guqinNote），缺什么补什么，别另起炉灶。
- 复用经验：「延迟展示」动画三件套模式=预计算+占位+setTimeout 揭晓，零侵入原逻辑，可推广到其他随机类面板。

## 塔罗优化七连（用户「1234567」，md5 f4a3b329，已部署 0279ad79；tab 26→27）
- **① 每日一塔罗**：tarot 面板顶部加「今日指引牌·每日更新」区块，按日期确定性取牌（dayN%78 + 7%10 逆位），全站同一天同一张；🎧 按钮用 SpeechSynthesis 朗读（zh-CN）。
- **② 关系阵**：**确认已存在**（tarotRelationBtn + TAROT_SPREADS.relation，还有爱情/事业/年运/五维/决策/时间流/复合等 10+ 牌阵），未重复添加。
- **③ 术语速查（T3+塔罗章节）**：新增 data-cat="tool" tab「术语速查」（典图标，tab 27），面板 41 词分三组：命理(四柱/真太阳时/十神/大运/纳音/命宫/四化…)、占卜(本卦变卦/世应/用神/体用/奇门八门/旬空…)、塔罗西式(大阿卡纳/四元素/正逆位/愚者之旅/凯尔特十字/雷诺曼/灵数/MBTI…)。.term 样式；home 标题改动态计数。
- **④ 逐张翻牌**：runSpread 与三牌阵的 setCard 改 setTimeout(i*240) 交错；runSpread 结果渲染顺延至最后一张翻完（(n-1)*240+400）。
- **⑤ 纸墨渐显**：全局 .result{animation:paperIn .38s}（opacity+translateY 微妙浮现）。
- **⑥ 关系能量环**：关系阵结果尾部加双圆交叠 SVG（你/TA 元素按塔罗元素→五行映射），相生/相克/相同五条文案。**坑**：标志必须在 setTimeout 调度时捕获（const _relRing=!!window.__relRing），否则执行时已被重置。
- **⑦ 语音今日签**：并入 ① 的 🎧 按钮（SpeechSynthesis，同语音输入技术栈）。
- 验证：JS 语法 OK、线上 tab27/术语41/每日塔罗/能量环/逐张翻/paperIn 全在、五端一致。
- 经验：改 runSpread/三牌阵这类"双胞胎结构"函数时，替换锚点必须带上下文行区分，否则匹配数=2 断言失败。

## 术语速查重组为中西 + 扩充（md5 a49e32bd，已部署 0279ad79）
- 用户要求"术语速查分为中西，继续扩展内容"。从 41 词扩到 **81 词**，四大节（python 整块替换 terms 面板，marker 定位 id="terms" 到 hint 之间）：
  - 【中】命理·八字紫微（28 词）：新增 藏干/五行生克/十二长生/刑冲破害/三合六合/流月流日/起运换运/格局/调候/神煞/天乙贵人/桃花/紫微十二宫/星曜/大限/三方四正…
  - 【中】占卜·卦签奇门（25 词）：新增 阴阳/八卦/爻/六亲/六兽/飞神伏神/小六壬六神/奇门八神/三奇六仪/九宫天盘地盘/十二建除…
  - 【西】塔罗（15 词）：新增 四组牌/宫廷牌/大阿卡纳分段/洗牌切牌/每日一牌/牌与星座对应/直觉解读…
  - 【西】占星·灵数（13 词）：新增 太阳/月亮/上升星座/行星/宫位/相位/水逆/主数天赋数/生命路径/荣格八维…
- 中/西徽章：.cw-cn(金底) / .cw-west(蓝底) 标在节标题前，一眼区分中西。
- 验证：JS 语法 OK、线上 81 词/徽章/新词抽查全在、tab 27、五端一致。
- 经验：整块替换 HTML 用 python marker 定位（index + slice）比 Edit 大字符串更稳。

## 移除术语速查（md5 1985e14e，已部署 0279ad79）
- 用户反馈"这个有什么意义吗，移除吧"——术语速查整个功能（81 词四大节）移除：tab 行、面板块、CSS（.term flex 布局/.term b/.term span/.cw 徽章 6 行）。
- **保留**：900 行 `.term{position:relative...}` 是结果里术语 tooltip（wrapTerms）样式，与速查页无关，不能删。
- 九宫格 homeGrid 本来就过滤 tool 类（terms 不在卡里），homeTitle 动态计数不受影响；tab 27→26。
- 经验：删功能时先区分同名 CSS 的两套用途（wrapTerms tooltip vs 页面布局），只删目标一套。
