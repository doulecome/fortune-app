# 2026-08-15 占卜应用深化日志

## 解梦模块重建（消除「牛头不对马嘴」）
- 根因：旧 dreamBtn 用 `for...break` 只取第一个命中意象，整段梦境只返回最靠前那一个孤立解读；词库缺口大、无结构化、无情感锚点。
- 改动：
  - `const DREAMS`（对象单命中）→ `const DREAM_LIST`（60+ 类带 `cat` 数组：人物/情绪/变动/新生/状态/动物/自然/场景/动作/物品），大幅扩充意象。
  - dreamBtn 重写：收集全部命中 → 按类别优先级排序（人物>情绪>变动>…）→ 结构化输出（意象拆解 / 综合心解 / 小建议），接可选「梦中感受」单选（平静/喜悦/害怕/焦虑/困惑/说不清）。
  - ui.html：dreamWord 改 `textarea`（支持整段描述），加感受选择区。
- 验证：探针 9 断言全过（多意象聚合、命中具体意象、兜底结构化均正常）。

## 奇门补投资/置业/开业三场景
- QM_SCENE_TXT 加 投资/置业/开业（tip/sp/act 各 2 变体，占位 {门}{星}{神}{sh}{ma}{kong}）。
- qimenSceneLine 的 USE/PO/extra 补三场景（投资用神生门+玄武防骗；置业生门+九地稳购/玄武查档；开业开门+天辅招牌/白虎口舌）。
- ui.html `qmScene` 下拉加三项。

## 构建 / 同步 / 部署
- `node --check` 通过；`build.js` 生成 fortune.html/index.html 946 KB（app 段 362251 B）；同步 deploy/index.html + wechat-miniprogram/fortune.html（三端 1200453 字节一致）。
- 探针 `_verify17.js` 9 项断言 `ALL_PASS=true`，已清理。
- 复用沙箱 `59299e91e4e74600b4020b8c01d9f5b1` 部署，`verified:true`。
- 线上地址：`https://59299e91e4e74600b4020b8c01d9f5b1.app.workbuddy.link`

## 解梦「更准」：极性 + 主体识别引擎（app.js ~2356 起）
- 新增 `DREAM_POL`（15 个极性敏感标题的 顺/滞 双解读映射：财/资财/解结/水/雨/火/考运/失机/前程/行旅/压力/失落/蛇/耗损/婚合）、`DREAM_SUBJ`（掉牙/婚合/孕育/耗损）、`POL_POS/POL_NEG/SUBJ_OTHER` 常量、`DREAM_NEGDEFAULT`（失机/失落/耗损/前程/行旅/压力）。
- 重写 dreamBtn：① 局部窗口极性（匹配词前后 ±8 字取 seg、±3/±5 字取 没/不/未/别/无 否定）`polOf`——"捡到钱"→顺、"丢钱/钱包被偷"→滞、"考试没考过"→滞（修正 `考[上过]` 误把"考过"当正面，后缀否定翻转）；② `negNear` 含后缀，使"没迟到/没考过"正确翻转为顺；③ `subjOf` 识别"别人/奶奶/老公…"→标注"梦里是别人经历"；④ 去重、按顺/滞标 〔顺〕〔滞〕、综合心解点名意象+净极性(顺/滞/掺半)+感受、单意象给覆盖度提示。
- 构建：app 段 365305 B、fortune.html 949 KB；三端 1205457 字节一致。
- 探针 `_verify18.js` 7 项 `ALL_PASS=true`（捡钱顺/钱包被偷滞/考试没考过+焦虑滞/奶奶掉牙别人/赶上车+开心顺/没迟到顺/单意象提示），已清理。
- 复用沙箱 `59299e91e4e74600b4020b8c01d9f5b1` 部署，`verified:true`。
