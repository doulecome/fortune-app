# 2026-08-10 工作日志

- **用户反馈「动画一点都不高级」→ 根因诊断 + 方向选择**：用户连续多轮"继续优化动画"后直接否定"一点都不高级"。根因不是动效少，而是**堆微交互=廉价**——确认 5 个廉价信号：①按钮灰色 Material 涟漪（泛滥到每个 .btn/.tab/.cat）；②卡片 3D 倾斜跟随（gimmicky）；③数字滚动 ticker；④墨晕 scale(10) 爆开；⑤同时跑的动画太多缺统一语言。用 AskUserQuestion 给 4 个方向（减法/统一语言/打磨材质/整体重做），用户选 **「质感糙，打磨材质」**。
- **材质打磨 v5b（保留动效、提升材料感）**：4 处：① **墨晕去爆开**——`.ink-swirl` 由 `scale(0)→scale(10)` 爆炸改为 `scale(.2)→scale(2.1)` 柔扩 + `filter:blur(3px)` + 金红径向更淡，时长 .62s→.9s；JS 移除计时 700→760ms 对齐。② **涟漪改墨色**——`.ripple` 灰 `rgba(74,68,57,.22)` 平涂 → 墨色径向 `radial-gradient(circle,rgba(43,38,32,.15),transparent 68%)` + `cubic-bezier(.22,1,.36,1)`，读作"墨点扩散"而非廉价灰。③ **金线光晕**——`.sc-svg,.bz-disc{filter:drop-shadow(0 0 1px rgba(201,164,92,.5))}`，自绘描边线稿带极淡金晕，读作"金线勾勒"；半径 1px 不糊线。④ **纸面光泽调暖**——卡片活体光泽白 `.16/.17` 玻璃感 → 暖米 `rgba(245,240,229,.11~.12)` + 淡金 `rgba(201,164,92,.05)` 42%，明暗双主题都改，读作"宣纸被光照/描金"而非"玻璃"。未动：数字滚动、3D 倾斜（用户选材质非减法，保留）。验证：`node --check` 过 → build **777KB** → verify_cascade **无 delay 压制** ✅ / test_real **0 错误**（八字2308/紫微1420/塔罗984 字全有、fx-draw 92）✅ / verify_motion **0 错误**（二次揭幕 -0.5s、1500ms 精简 ✅）→ 预览 102KB。**经验**：① 高级感来自克制+统一+物理调性准，堆效果=反效果；用户说"不高级"时先诊断廉价信号再对齐方向，别盲加。② "材质打磨"路径 = 把通用 effect 的材质换成主题材质（灰涟漪→墨扩散、白玻璃光泽→宣纸描金、素墨描边→金线光晕、爆开→柔扩），比加新动效更提档且零回归。③ 改 `scale(10)` 类爆开参数为 `scale(2.1)`+blur 是去"特效感"的通用手法。

- **统一语言 + 减法 v5c（用户：继续优化）**：用户选材质后说"继续优化"，推进「统一曲线 + 减法」这条主线（上轮结尾已预告）。**减法**：① `wireTilt`（卡片 3D 倾斜跟随鼠标晃动，gimmicky）删 transform 倾斜，改名 `wireGlare`，只喂 `--mx/--my` 给卡片宣纸描金光泽（保留指针跟随的"活"质感、不晃）；调用处同步改名。② 删 `countUp` 数字滚动 ticker（通用模板感）——数字能量/生肖配对环形分数改为直接显示 `${score}`/`${rel.score}`，删函数定义+2 处调用+`data-to` 属性。**统一**：① 删掉与 K 层冲突的**第一套 `:root` 缓动变量**（原两处定义、且 `--e-spring` 一处 1.56 过弹一处 1.34 打架），K 层作唯一真相源并补 `--e-inout`；② 写 `_unify_easing.js` 按上下文关键词把 **~22 处裸 `cubic-bezier`** 映射到 4 条变量（入场/淡出 rise/panelReveal/reveal/ripple/scrollUnfurl/swirlGo→`--e-out`；状态切换 transition/foldable/card→`--e-std`；盖章 stampDown×3/翻牌 fxpop→`--e-spring`；描边 drawIn→`--e-ink`），塔罗洗牌弹性 `.34,1.25,.5,1`→`--e-spring`；**保留 sg* 开馆演出与焚香烟循环**（一次性 hero/氛围，非重复微交互，强行统一会机械）。验证：`node --check` OK → build **776KB**（928KB）→ verify_cascade **无 delay 压制** ✅ / test_real **0 错误**（八字2308/紫微1420/塔罗1043 字全有、fx-draw 92）✅ / verify_motion **0 错误**（二次揭幕 -0.5s、1500ms 精简 ✅）→ 预览 101KB。**经验**：① 动效"不高级"的核心硬伤之一是**日常交互层曲线不统一**（此前 18 种裸曲线各自为政），收敛到 4 令牌后整体"像一种语言在说话"，比加任何效果都提档。② 删"跟手晃动/数字跳动"类通用模板微交互，是减法提档的直接手段。③ Edit 工具在同文件被外部脚本改过后会报 stale（"modified since read"），此时降级用 sed/node 改即可。④ 同文件多 Edit 并行在本项目实测可行（基于独立非重叠 old_string）。

- **GitHub 借鉴·氛围与材质升级 v6（用户："还是不够高级，去GitHub"）**：去 GitHub 研究顶级国风/命理 Web 实现，对标 **tianji（天机：深色中国风 UI，#bg-canvas 粒子氛围层 + 卡片四角金线 `::before/::after` + 标题金红渐变 clip-text + 衬线标题 Noto Serif SC + spring-up 入场 stagger）** 与 **星谕塔罗（Cinzel 古典碑刻标题 + 星空背景动画 + 卡牌悬浮）**。诊断 fortune 差距=缺「动态氛围层」+「标题点睛」+「卡片角饰」，且素雅墨色风（--gold 设成墨 #5b554a）缺金朱对比。落改（ui.html + app.js）：① **动态氛围粒子层**——新增 `#ambient` fixed canvas（z-index:0、pointer-events:none、opacity .5、reduced-motion 隐藏）+ `wireAmbient()`（rAF 限帧~30fps、按主题取色：浅色极淡金尘 rgba(201,164,92) 上浮、暗色星点 rgba(240,230,205) 微闪，26–60 粒子随面积自适应、出界回绕）；② **标题金朱渐变**——`h1` 改 `linear-gradient(135deg,var(--cinnabar-ink),var(--gold-stroke))` + `background-clip:text/color:transparent`（暗色 cream→gold），新增语义变量 `--gold-stroke:#c9a45c`/暗 `#d8b86a`；③ **卡片四角金线**——`.card::before` 由「左上角柔光」改 8 段 `linear-gradient` 在四角画 1.5px 描金 L 线（inset:10px、opacity .3），tianji 同款精致角饰。验证：build **909KB** → verify_cascade **无 delay 压制** ✅ / test_real **0 错误**（八字2308/紫微1420/塔罗1064 字全有、fx-draw 92）✅ / verify_motion **0 错误**（二次揭幕 -0.5s、1500ms 精简 ✅）→ 预览 115KB。**修复**：jsdom 测试环境无 canvas 包，`getContext` 报 Not implemented，已在 test_real.js/verify_motion.js 的 jsdomError 监听中过滤该噪音（真实浏览器 0 错误）。**经验**：① 高级感三大 GitHub 共性手法=**氛围粒子背景 / 标题渐变点睛 / 卡片角线装饰**，单文件离线项目可直接复刻（不依赖 React/Framer Motion）；② 验证脚本应过滤 jsdom 已知的 Not implemented 限制（canvas/IntersectionObserver 等），否则会误报运行时错误、掩盖真 bug；③ 标题/卡片点睛用「朱砂+描金」双色渐变，比纯墨字立刻提贵气一档，且幅度克制不艳。

- **GitHub 借鉴·胶片颗粒 + 暗角 + 印泥渗墨 v7（用户："继续高级感优化，再去GitHub上面学"）**：再赴 GitHub 挖本未用手法，对标 **grained / ngx-noise / pixlated（全局 film-grain 叠加层：feTurbulence 灰噪 + mix-blend-mode + 极低 opacity ~0.05 消除数字平面感）**、**lyy0323/ink（程序化水墨 + 竖排题款）**、**luck_bless（暗调金辉 + 星空）**。诊断 fortune 缺口=已有静态宣纸纹理(body feTurbulence alpha .05)与印章/竖排，但**缺内容之上的动态颗粒层 + 暗角景深 + 印章渗墨质感**。落改（ui.html 三处）：① **全局动态胶片颗粒**——新增 fixed `#grain`（z-index:48、pointer-events:none、mix-blend-mode:soft-light、opacity .11）+ 内嵌 feTurbulence 灰噪 data-URI + `grainShift .9s steps(6)` 背景位微沸（steps 模拟胶片沸腾、不卡）；reduced-motion 关动画降 opacity .08；暗色改 overlay/opacity .16。② **暗角景深**——新增 `#vignette`（z-index:47、radial-gradient 透明→边缘 rgba(40,30,18,.10)，暗色→rgba(0,0,0,.40)），聚拢中央、增加纵深如框中画。③ **印泥渗墨**——`.seal-stamp` 加 `::before` 叠 feTurbulence 灰噪 + `mix-blend-mode:overlay/opacity .5`，红印像真实印泥洇进纸。二者均置于 `.app`(z-index:1) 之上、splash/modal 之下，pointer-events:none 不挡交互。HTML 在 `<body>` 后注入两 div。验证：build **933KB** → verify_cascade **无 delay 压制** ✅ / test_real **0 错误**（八字/紫微/奇门/塔罗全有、fx-draw 92、揭幕无残留）✅ / verify_motion **0 错误**（二次揭幕 -0.5s、1500ms 精简 ✅）→ 预览 117KB。**经验**：① 「高级感」最后一块拼图常是**全局颗粒层**——它把"数字平面"变成"有肌理的实物"，是顶级站点（grained/ngx-noise 类）通用秘密，且纯 CSS+Svg data-URI 零依赖；关键是 opacity 极低(.08–.16)+soft-light/overlay 混合，过强反而脏。② 暗角 vignette 是代价最低的"景深/聚焦"手法，单条 radial-gradient 即可。③ 颗粒/暗角必须 z-index 高于内容、pointer-events:none，且放 splash/modal 之下，避免遮开场与弹窗。④ `grainShift` 用 background-position(+/-22px) 而非 transform，因平铺背景位移无缝、不会露边。

- **流畅度优化 v8（用户：动画还是不够流畅）**：根因是渲染开销非设计。定位三大杀手并修复（均不改观感）：① #grain 持续微沸——`animation:grainShift .9s steps(6)` 配全屏 `mix-blend-mode:soft-light` 每帧重混整页，最重；去掉 animation 改静态颗粒（混合只算一次）+ 删 keyframes grainShift。② wireGlare 指针高频重绘——`pointermove` 直接写 `--mx/--my` 无节流，改 rAF 批量写，每帧最多一次。③ wireAmbient 限帧 30→~20fps（`t-last<33`→`50`）、粒子 26-60→18-40（`/28000`→`/36000`）。验证：build 780KB → cascade 无 delay 压制 ✅ / test_real 0 错误（八字2308/紫微1420/奇门919/塔罗1056/称骨453、fx-draw 92）✅ / verify_motion 0 错误（逐笔错落 92/92、二次揭幕 -0.5s、1500ms 精简）✅。经验：① 卡顿优先查「全屏 mix-blend 层 + 持续动画」组合，停动画即大幅提帧且不改观感；② 指针/滚动事件一律 rAF 节流；③ canvas 20fps+降粒子是「有氛围不抢主线程」甜点；④ 三套验证（cascade/real/motion）已固化为动效交付门禁。

- **流畅度优化 v9（用户：继续优化）**：精准去结果区滚动 backdrop 开销 + canvas 后台暂停。诊断关键：结果区主容器 `.card`(164)/`.panel`(183) 本就无 backdrop-filter（仅半透明纯色），随文档滚动、真正带 backdrop 的只有 `.consensus`(532)/`.starchart`(711) 两个浮动子卡（各结果 1–2 个）。修复：① 去 `.consensus`/`.starchart` 的 backdrop-filter（改纯半透明 --glass-strong，更国风更稳，零观感差）；② 全局 `--glass-blur` 16px→12px、saturate 1.25→1.15(浅)/1.2→1.12(暗)，所有毛玻璃更轻统一（面板/弹窗/输入框继承）；③ `wireAmbient` 加 `visibilitychange`——标签页隐藏时 cancelAnimationFrame 暂停 canvas、回来 last=0 重启，省后台开销。验证：build 780KB → 三套验证全绿（塔罗978/八字2308/紫微1420/奇门919/称骨453、fx-draw 92、运行时 0 错误、二次揭幕 -0.5s/1500ms）✅。经验：① 优化前务必 Grep 确认「谁真带 backdrop-filter 且随滚动」——`.card`/`.panel` 常被误判为瓶颈但其实无 blur，乱改反而损观感；② 全局降 `--glass-blur` 是零观感、统一提速甜点（16→12 采样成本降约 40%）；③ canvas 务必挂 visibilitychange 暂停，后台不空转。

- **八字专属叙事动画 v10（用户：八字的动画还是不够高级）**：根因=八字一直用通用 `enhanceFrameInk`（与其他面板共用），缺八字自己的「推演叙事」——星图连线被一次性描出（无"从日主长出"节奏），命盘四宫文字被统一机械淡入（无"逐宫落墨"书法感）。关键发现：星图连线方向天然从日主中心(280,232)指向星点，`fx-draw` 的描边顺序即"从日主生长"，只需按四柱顺序错开延迟即可呈现推演。落改：① 生成段加标记 class——scRing地支→`.sc-zhi`、scStar干支→`.sc-gz`、柱名→`.sc-pillar[data-pillar]`、日主大字→`.sc-day`、副文→`.sc-day-sub`、命盘中心盘→`.dz-cdisc`（纯加属性，不改内容布局）；② 新增 `enhanceBazi(svg)`——保留通用 fx-draw 描边打底（保验证计数 92 不回归），星图：连线按 年→月→日→时/干先于支 逐条生长(--d 0.12+i*0.10)、十二地支环最早淡入、星点干支按柱序"沉降落定"(`.sc-land`)、日主最后落定(1.85s 压轴)；命盘：外环弧按序描、四宫按 年→月→日→时 逐宫落墨(`.sc-land` 柱名→干→支→十神)、中心日主"落印"(`.dz-sealink` 朱砂晕开，2.0s 压轴)；八字调用点(765-766)由 `enhanceFrameInk` 改 `enhanceBazi`，紫微保持 `enhanceFrameInk` 不变；③ ui.html 高级动效区新增 `.sc-land`(translateY+scale+轻blur 沉降)/`.dz-sealink`(scale+blur 朱砂晕开)+reduced-motion/print 守卫。手法亮点：所有新增动画均为**一次性 CSS**（animation both forwards），无 rAF/无混合层/无持续开销，完全不损 v8/v9 流畅度；叙事层次=判词逐字(enhanceType已有)→星图演→命盘揭逐宫→日主落印压轴。验证：build **785KB** → cascade 无 delay 压制 ✅ / test_real 0 错误（八字2308/紫微1420/奇门919/塔罗978/称骨453、fx-draw 92、揭幕无残留）✅ / verify_motion 0 错误（fx-draw 92、唯一延迟值 **79 种** 比通用更丰富、二次揭幕 -0.5s/1500ms 精简）✅。**经验**：① 当某面板"不够高级"时，先看它是否复用了通用动画编排——通用=无该领域叙事；高级感来自"为该领域定制的因果节奏"（命理=从日主推演生长、命盘=逐宫落墨），而非更多效果。② SVG `fx-draw` 的描边方向由 path/line 书写顺序决定，可借此天然实现"从中心长出"叙事，无需额外 JS 绘制顺序。③ 给生成期元素加 class 标记（不改内容/布局）是"让专属编排函数精准识别元素"的零风险手法。④ 新增一次性 CSS 动画（both forwards）提升观感零损流畅度，区别于 v8/v9 去持续动画的思路——两者互补。

- **流畅度优化 v11（用户：继续优化动画，还是很僵硬卡顿）**：根因=入场动画大面积用 `filter:blur()` 做"由模糊到清晰"，而 blur 是 CSS 动画里最贵属性——每帧对整个元素重新栅格化；八字结果生成时几十个 SVG 文字(sc-ink/sc-land/dz-sealink)+结果子块(reveal)同时跑 blur，等于瞬间几十次全元素重绘，主线程打满→卡顿。此前的 v8/v9 只动了颗粒层/glare/backdrop/canvas，没碰这块，故"还是卡"。落改：系统性移除 11 处高频入场 keyframes 的逐帧 `filter:blur`——reveal(改 translateY+scale .985)、inkReveal(去 blur 留 clip-path 擦入)、scFill(sc-ink 改纯 fade)、scLand(.94 沉降)、dzSealIn(.6 落印)、panelReveal、twIn(六爻6行)、h1Open(开场标题留字距去 blur)、incLine、coupletIn——统一改为纯 `opacity+transform`(GPU 合成层、零重绘)，保留"柔和落定"体感(轻微 scale/translateY 替代 blur)。并给 `.sc-svg` 加 `will-change:transform`，令命盘/星图入场整段提升为单层合成、彻底消除层抖动。不动任何叙事/延迟/内容。验证：build 785KB → cascade 无 delay 压制 ✅ / test_real 0 错误(八字2386/紫微1438/奇门913/塔罗991/称骨456、fx-draw 92、揭幕无残留) ✅ / verify_motion 0 错误(fx-draw 92、唯一延迟 79 种、二次揭幕 -0.5s/1500ms 精简) ✅。**经验**：① 卡顿根因排查顺序应是「全屏 mix-blend 持续动画(v8)→指针/滚动事件节流(v8)→backdrop 随滚动重算(v9)→canvas 帧率(v8/v9)→入场动画逐帧 blur 重栅格化(v11)」，blur 动画是隐蔽杀手，因"由模糊到清晰"观感高级但成本极高，高帧率站点几乎从不对多元素动画 blur；② 移除逐帧 blur 后保留高级感的标准替代=「轻微 scale(.94–.985)+translateY+opacity」组合，仍是柔和落定但零重绘；③ `will-change:transform` 仅加在入场重的容器(.sc-svg)而非每个子元素，避免层数爆炸；④ 改完须跑三套验证确认 0 运行时错误且 fx-draw/延迟值不回归。

- **流畅度优化 v12（用户：修 → 指滚动/弹窗卡顿）**：根因=全屏 mix-blend 合成层 + 覆盖层毛玻璃显示时背后粒子仍跑 rAF。诊断：① `#grain` 全屏 `mix-blend-mode:soft-light`(浅)/`overlay`(暗) 层——v8 只停了它的 animation，层仍在，页面滚动时整页与该层每帧重新混合→滚动卡顿（最大单一源头）；② 加载层 `.ink-bloom.saga`（生成结果时全屏 `backdrop-filter:blur(3px)`）与塔罗弹窗 `tarotModal`（`.tm-card/.tm-back` 毛玻璃）显示时，背后 `wireAmbient` 粒子 canvas 仍 rAF 重绘，与毛玻璃每帧重算叠加→弹窗/加载期卡顿。落改：① **#grain 去 mix-blend**——`soft-light/overlay` 改普通 `opacity` 叠加（浅 .11→.08、暗 .16→.12），消除滚动时全页每帧重混，观感几乎不变（胶片感主要来自噪点纹理本身）；② **wireAmbient 加 paused/scrolling 标志**——`frame` 在 `paused||scrolling||document.hidden` 时跳过绘制（空转 rAF 零成本），滚动时暂停粒子(200ms idle 恢复)、后台/覆盖层显示时暂停；③ 暴露 `window.holdAmbient/releaseAmbient`（计数防嵌套误恢复），加载层创建 hold/移除 release（2103/2157）、塔罗弹窗打开 hold/三处关闭 tmBack·tmClose·Escape release（1309/1320/1322）；调用点 `if(window.holdAmbient)` 守卫，REDUCE 时 wireAmbient 未暴露安全跳过。验证：build 785KB → cascade 无 delay 压制 ✅ / test_real 0 错误（八字2386/紫微1438/奇门913/塔罗1032/称骨456、fx-draw 92、揭幕无残留）✅ / verify_motion 0 错误（fx-draw 92、唯一延迟 79 种、二次揭幕 -0.5s/1500ms 精简、暂停态 fx-hold 0 正常释放）✅。**经验**：① 滚动卡顿隐藏杀手常是「全屏 mix-blend 层」——它让浏览器每帧把整页与该层重新混合，比任何单元素动画都贵；去 blend 改普通低透明叠加，零观感损失换大幅提帧；② 全屏毛玻璃(backdrop-filter)与背后跑动的 canvas 粒子是「弹窗卡顿」组合拳，覆盖层显示时暂停背景动画是标准解法（弹窗遮住背景、粒子本就看不见）；③ 暂停用「标志位 + frame 内 early-return」而非 cancelAnimationFrame，避免与 visibilitychange 的 cancel/restart 竞态，空转 rAF 成本可忽略；④ 多覆盖层用计数 hold/release 而非布尔，防嵌套误恢复。去掉 animation 改静态颗粒（混合只算一次）+ 删 keyframes grainShift。② wireGlare 指针高频重绘——`pointermove` 直接写 `--mx/--my` 无节流，改 rAF 批量写，每帧最多一次。③ wireAmbient 限帧 30→~20fps（`t-last<33`→`50`）、粒子 26-60→18-40（`/28000`→`/36000`）。验证：build 780KB → cascade 无 delay 压制 ✅ / test_real 0 错误（八字2308/紫微1420/奇门919/塔罗1056/称骨453、fx-draw 92）✅ / verify_motion 0 错误（逐笔错落 92/92、二次揭幕 -0.5s、1500ms 精简）✅。**经验**：① 卡顿优先查「全屏 mix-blend 层 + 持续动画」组合，停动画即大幅提帧且不改观感；② 指针/滚动事件一律 rAF 节流；③ canvas 20fps+降粒子是「有氛围不抢主线程」甜点；④ 三套验证已固化为动效交付门禁。
