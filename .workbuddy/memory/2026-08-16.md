# 2026-08-16 工作日志

## 解梦跨模块联动（日主五行本命视角）
- 需求：若用户在「幸运色/五行宜忌」填过生日，解梦对水/火类梦做本命视角解读。
- 实现：
  - luckyBtn（app.js:4706 后）点查询时把 luckyPersonalize 算出的日主五行写入 `window.__userDayWu` / `window.__userDayGan`（无生日则置 null）。
  - 新增 `DREAM_WU={'水':'水','雨':'水','雪':'水','火':'火'}` 与 `dayWuDreamNote(de,ue)`（app.js:2391 后），按五行生克给五类关系解读：比和同气 / 印来生我（生我）/ 我生食伤（泄气）/ 官杀临身（克我）/ 我克为财。
  - dreamBtn 命中水/火意象且 `window.__userDayWu` 存在时，生成「本命视角」段落（含日主标注、各意象关系句），并联动副标题「本命视角已联动」+ 底部注；无生日则完全不出现。
  - ui.html 解梦面板加提示行，说明联动来源（幸运色填生日）。
- 构建：`node --check` 通过；build.js 生成 fortune.html/index.html（939 KB / 1210373 B），同步 deploy/wechat-miniprogram 三端一致。
- 探针 `_verify20.js` 5 项 ALL_PASS=true：水梦×木日主→印来生我、火梦×木日主→我生食伤、未填生日无联动、幸运色填生日(1990-05-20)→写入日主「木」、联动后水梦带本命视角。已清理。
- 部署：复用沙箱 59299e91e4e74600b4020b8c01d9f5b1，verified:true。

## 解梦能力演进回顾（8/14–8/16）
多意象聚合 → 顺/滞极性+自己/别人 → 生活领域+一句话钩子 → 本命视角跨模块联动。始终趣味占卜、非预言。

## 本命视角接真实旺衰 + 星座三要素（续上条需求）
- 需求：① 把本命视角从「幸运色·扶抑近似」升级为八字完整盘的「真实旺衰喜用」；② 星座扩充上升/月亮星座 + 本命三要素。
- 实现：
  - 抽离共享引擎 `baziStrength(y,m,d,h)`（app.js 八字 handler 前）：四柱生克 + 月令提纲加权，算 `qiang/用神 yongUniq/忌神 jiUniq`（与八字面板同源，取代原扶抑近似）。
  - 八字面板 `baziBtn` 改调 `baziStrength` 并把真实结果写入全局 `window.__baziDayGan/__baziDayWu/__baziQiang/__baziYong/__baziJi`（向后兼容 `__userDayGan/__userDayWu`）。
  - 修复隐患：原面板 `DZ_BENQI/CANG_GAN` 定义在 handler 内，重构后 `monthGe`(line 796)、`CANG_GAN`(line 1023) 引用失效；已把 `DZ_BENQI/CANG_GAN` 提为全局常量（与 ZHI 等同区），baziStrength 改引用全局。
  - 幸运色 `luckyPersonalize` 增加可选时辰参数：有时辰→走 `baziStrength`（真实旺衰·喜用+忌）；无时辰→保留扶抑近似。UI 加「出生时辰」下拉。luckyBtn 渲染喜用色 + 忌色，并写入 `__baziYong/__baziJi`。
  - 解梦 `benmingBlock` 优先用真实 `__baziYong/__baziJi`：水/火类梦标注「喜用/忌神」，无真实盘面时回退到日主五行生克（仅幸运色填过生日）。
  - 星座：新增 `moonSign(y,m,d,h)`（月黄经近似公式）、`ascendantSign(sunIdx,hh)`（出生时间通俗近似）；UI 加「出生时间」输入。starBtn 有填时间→渲染 太阳/月亮/上升 + 三要素综合；未填→仅太阳 + 引导文案。
- 构建：build.js 生成 fortune.html/index.html（958 KB / 1218377 B），三端同步一致。
- 探针（jsdom）全绿：baziStrength/moonSign/ascendantSign 均为函数；真实旺衰样本(1990-08-15 午)→日主壬水·中和·喜土火木·忌水金；幸运色填时辰→真实旺衰(有忌)、不填→扶抑近似；解梦水/火梦→本命视角带喜用/忌神；星座填时间→日/月/升/三要素齐全、不填→无月升块。errors:[]。临时 probe 文件已清理。
- 部署：新沙箱 0279ad797a3b478488df88c02cee5abe，verified:true，URL https://0279ad797a3b478488df88c02cee5abe.app.workbuddy.link 。
- 注意：八字 hour `<select>` 仅含奇数值(0,1,3,5…23)，探针若设偶数(如12)会取不到→h=NaN；真实用户从下拉选，默认子时=0，无此问题。

## 八字命盘位置调整（用户：全部命盘/占卜/运势应在八字上面）
- 需求：八字命盘原是「中国占卜」分类的第一个(active)标签，用户认为应置于全部命盘/占卜/运势模块之下。
- 实现：ui.html `.tabs` 内把 `八字命盘` 标签从首个移到 `cn` 分类末尾（时辰吉凶之后、塔罗之前）；默认 landing 改为首个 `cn` 标签 = 姓名五格（改其 tab 为 active、name 面板加 show；bazi 面板去掉 show）。
- 机制确认：`filterTabsByType(type)` 取 `TABS.find(首个同分类且可见)` 为 landing，故首标签顺序决定默认面板；tab 通过 data-p 引用面板 id，面板 DOM 顺序不影响显示。
- 构建+同步+部署：沙箱 0279ad797a3b478488df88c02cee5abe 复用，verified:true。探针确认 cn 顺序末位=八字命盘、点击八字仍正常切换。

## UI 层级修正（用户：八字放第一个 + 大筛选应在小筛选上面）
- 需求：① 八字命盘放回第一个(active)标签；② 导航层级反了——粗筛「全部/命盘/占卜/运势」(`.subcats`) 在下面，而具体模块标签(`.tabs`)在上面；用户要求粗筛在上、具体在下。
- 实现：ui.html 把 `.subcats` 整块从 `.tabs` 之后移到 `.tabs` 之前（header 之后）；`.tabs` 内把 八字命盘 移回首位并设 active，name 标签去 active；bazi 面板恢复 `show`、name 面板去 `show`。
- 验证（jsdom）：`#subcats` 仅 1 个且 DOM 位置在 `#tabs` 之前；八字为首个 cn 标签且 active。
- 构建+同步+部署：沙箱 0279ad797a3b478488df88c02cee5abe 复用，verified:true。

## 交互优化（用户：继续优化交互）
- 4 项：① 子类筛选 chips（命盘/占卜/运势）再点当前激活项=取消筛选回「全部」(`_curSub` toggle)；② `filterTabsByType` 激活首匹配标签后 `scrollIntoView` 居中（筛选后激活标签不再跑出可视区）；③ `navSave` 新增持久化 `sub`，init 时 `filterTabsByType(initType, initSub)` 还原上次筛选；④ 标签条溢出滚动：ui.html 用 `.tabs-wrap` 包裹 `#tabs` + ‹ › 箭头按钮，CSS `.tabs-wrap.can-scroll .tabs-nav{display:flex}`，`updateTabScroll()` 按 scrollWidth/clientWidth 切换；并加键盘 ← → 切换可见标签（输入框/弹窗内不触发）。
- NAV_KEY='xuanji_nav_v1'（注意：探针曾误读 xuanji_nav 导致空值，已校正）。
- 验证（jsdom probe5）：hasWrap=true/arrows=2；点命盘→仅 ming+tool 可见且 active=bazi、navSub=ming；再点命盘→21 全部可见；点紫微→nav={cat:cn,p:ziwei,sub:all}；ArrowRight 由 ziwei→qimen。errors:[]。
- 构建+同步+部署：沙箱 0279ad797a3b478488df88c02cee5abe 复用，verified:true。

## 参数/历史 & BGM 优化（用户问：参数跟历史每个都有吗 + bgm可以优化吗）
- 答复：参数设置是**全局统一**面板（紫微派别/奇门定局/八字侧重/琴音开关），非每模块独立；历史是**全局一份**(LS_KEY='xuanji_history_v1')，每模块有「保存此结果」按钮、每条带模块标签[type]，可回看/导出/复制。用户未要求做「每模块独立历史」。
- BGM 优化（app.js §24 古琴氛围音）：① 新增混响(反馈 DelayNode 0.30s + fb 0.34 + wet 0.34) 经 master 输出，琴音有庭院/殿堂空间感；② 新增低频音床 drone(G2 98 + 五度 D3 146.83 双正弦微失谐 + 0.05Hz LFO 呼吸，渐入0.016)；③ 旋律随机游走(_lastIdx 邻音上下)+ 偶发×2 八度 + 18% 概率留白，更连贯；④ master 加 DynamicsCompressor 防爆音 + 2s 淡入 / 0.8s 淡出。ensureGraph() 建图；guqinNote 改送 _master 与 _reverb.delay。
- 验证：node --check 通过；jsdom 加载 errors:[]，soundToggle 点击安全(no-op，jsdom 无 AudioContext)，guqinSaved 持久化正常；历史/设置/琴音面板均在。
- 构建+同步+部署：沙箱 0279ad797a3b478488df88c02cee5abe 复用，verified:true。
