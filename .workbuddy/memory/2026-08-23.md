# 2026-08-23 工作日志

## 新增功能：小六壬 + 诸葛神数（已上线）
- 用户指令「先添加小六壬跟诸葛神数」（在「中西两级分类 + 精简到低门槛」讨论背景下，先补全缺口）。
- **小六壬**（中式·占卜）：六神（大安/留连/速喜/赤口/小吉/空亡）+ 经典诀文 + 五行方位 + 谋事/婚姻/求财/失物/疾病/官讼分类断。算法=月上起日、日上起时 `((m-1)+(d-1)+(h-1))%6`。含「用今天」自动取当前月日时辰。
- **诸葛神数**（中式·占卜）：64 卦 × 6 爻 = 384 签完整体系。每卦含 卦名/六爻阴阳(bin)/基调/签诗/卦义；签号由报三数之和 mod 384 定；断语依爻位易理（初难二誉三凶四惧五功上变）。
- 实现方式：直接改 `deploy/index.html` 单文件（**源码 app.js/ui.html 是旧的、不含已做的 6 面板，重建会丢功能，故不能走 build**）。三处插入：tab(占卜组)/panel(iching 前)/JS(gyBtn 前)。
- 验证：内嵌 JS `node --check` 通过；DOM-stub 实跑算法——小六壬(3月15日午时)=小吉✅；诸葛(10,20,30)=第60签·履·上九✅；384 签遍历 0 异常；64 卦 bin 全合法；第1签=乾初九、第384签=未济上九✅。
- 四端同步 md5 = `f89a607e1ecb9140691761864c0fea7d`（deploy/index+fortune、index、fortune、wechat-miniprogram）。**注意**：只改了 deploy/index.html，需手动 `cp deploy/index.html deploy/fortune.html` 再同步根目录，否则 fortune.html 漏更新。
- 部署：CloudStudio 沙箱 `f514f7d7898c4d96bf784d8ea7429c58`，线上 /index.html md5 与本地一致 ✅。
- tab 总数 31 → 33。

## 待办（用户尚未拍板）
- 「精简到低门槛」方案仍悬而未决：A) 八字改极简模式 / B) 八字也隐藏；加诸葛 vs 不加（现为已加）。当前 33 tab。
- 用户此前偏好：合并重复（黄历/时辰/择日三合一、批量并入姓名、配对并入合婚、趣味四合一）、隐藏 5 个高门槛 tab（奇门/测字/解梦/赛博积德/北欧符文）。

## 关键坑（本项目专属）
- **源文件 vs 构建产物不同步**：app.js/ui.html 不含 6 面板/月份数字/新牌阵，只有 deploy/index.html(单文件) 是完整最新版。任何改动必须直接编辑 deploy/index.html，绝不能改源码后 `node build.js`（会丢失已做功能）。
- 线上校验打 `/index.html` 而非 `/`（Gateway 根路径偶发空体）。

### GitHub 已上线 + 上传包更新
- 用户已将项目推到 GitHub（自行操作，网页上传）。
- 此前准备的 `D:/fortune-upload/index.html` 是旧版 4aa6155（缺小六壬/诸葛）。已更新该上传包到最新 f89a607（与 deploy/ 一致，含小六壬+诸葛 2 个新 tab）。
- 若 GitHub 上仍是旧版，用 `D:/fortune-upload/index.html` 覆盖仓库根目录 index.html 即可。
- 精简（低门槛版）方案仍待用户拍板：A=八字改极简模式保留 / B=八字隐藏；tab 数 19~20（含小六壬，诸葛已加则 20）。

### 历史记录移入设置面板
- 应要求把「历史记录」从独立 tab 移进「设置」面板（右上角齿轮打开）。
- 实现：① 删 tab 导航项 `data-p="history"`；② 历史面板 HTML（histBtn/histClear/histExport/histCopy/histResult）从独立 `id="history"` 面板移入 settings 面板内部做子区块（保留所有按钮 id，JS 绑定照常）；③ switchPanel 加 `if(p.id==='settings'){ try{ renderHist(); }catch(e){} }` 实现打开设置即自动渲染历史。
- 验证：真机 eval 确认 renderHist 有记录渲染列表、空态文案正常、挂钩已挂载、无独立 history 面板；四端同步一致 md5 f9345f5；线上 /index.html 抓取 md5 一致、data-p=history 残留0、settings 内 histResult=1。
- 上传包 D:/fortune-upload/ 已同步到 f9345f5，GitHub 仓库若需更新用此文件覆盖根目录 index.html。

### 部署目标混乱排查（用户问 agentos-app.net 是不是网站）
- 用户书签 `https://0279ad797a3b478488df88c02cee5abe.gz4.agentos-app.net` 确为本项目线上站，但它是**最早那次部署的冻结沙箱**（md5 c3c28ce7 / 1476KB），缺小六壬/诸葛、历史仍是独立 tab——比本地 f9345f5 旧 2~3 个版本。
- 根因：项目存在**两个部署目标**——旧书签沙箱(0279ad79) + CloudStudio 沙箱(f514f7d7898c4d96bf784d8ea7429c58，即 app.workbuddy.link)。后续改动只推到了 f514f7d，旧书签没更新（沙箱不能原地更新）。
- 修复：四端同步到 f9345f5 后重新部署，CloudStudio 复用同一沙箱 f514f7d（未新建），新链接 `https://f514f7d7898c4d96bf784d8ea7429c58.app.workbuddy.link` 已验证 = f9345f5（XLR/ZHUGUA 均有、无独立 history tab）。
- 结论给用户：最新版在 f514f7d 链接，旧书签 0279ad79 是过期冻结沙箱应换书签；如需可 unpublish 旧链接避免混淆。

### 用户拍板「用旧的」→ 修复旧链接成功（重要方法）
- 用户选择继续用旧链接 `0279ad79.gz4.agentos-app.net` 当主站，要求更新它。
- **根因**：cloudstudio 部署按 `absolutePath` 匹配 deployTargetId（记录在 `~/.workbuddy/cloudstudio-deploy-history/*.json`）。C→D 迁移后：旧条目 afa154a567105e80 绑 C:\...\deploy→沙箱 0279ad79（gz4.agentos-app.net）；新条目 6e98dd9f1856f05b 绑 D:\...\deploy→沙箱 f514f7d（app.workbuddy.link）。C 盘路径没了 → 新部署全走新沙箱，旧 URL 冻结。
- **修复方法**：重建旧路径 `C:\Users\42134\WorkBuddy\2026-08-05-00-54-43\deploy`，放入最新 f9345f5 文件，再从该路径调用 deploy → 返回 sandboxId=0279ad79 ✅ 命中旧沙箱，旧链接更新成功。
- 验证：旧链接 XLR/ZHUGUA 均在、无独立 history tab。注意 agentos 网关 md5 与本地不同（2284e9b7 vs f9345f5，网关有包装），以功能标记为准。
- 现状：两个线上实例都是最新版（0279ad79 旧 + f514f7d 新），用户用旧的；f514f7d 为重复实例，可随时 unpublish。
- 附带发现：C 盘旧工作区仍在（含 .git，是 git 仓库），index.html/fortune.html 还停在 4aa6155。
