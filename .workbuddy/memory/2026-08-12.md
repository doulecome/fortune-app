# 2026-08-12 工作日志

- **v23 导航按「类型」分类（用户：把内容按类型分类啊，就是点击占比只会出现占卜的内容）**：将左侧分类导航从旧的 命盘/占卜/趣味/工具 四分类，重分类为与两套主题一致的类型轴——**中国占卜(cn)** / **国外塔罗(tarot)** / **工具(tool)**。每个 tab 的 data-cat 重写为：cn=全部中国命理/占卜面板(19 个：bazi,name,chenggu,ziwei,qimen,yuncheng,guanyin,dream,iching,liuyao,meihua,hehun,wen,num,merit,lucky,almanac,sizhi)；tarot=tarot,star；tool=settings,history。复用已有 showCat() 过滤逻辑（按 data-cat 设 display:none），点击类型只显示该类型标签：专项验证 国外塔罗=star,tarot ✅、中国占卜=18 个中国面板 ✅、工具=history,settings ✅。默认首类 cn（八字）。旧 localStorage 存的 ming/zhan/fun 在初始化兜底逻辑中自动回落到 cn，无断裂。主题切换(v22 中国占卜/国外塔罗)保持独立手动控制、未与分类联动（用户若想要「选类型即换肤」可再加）。构建 810KB，三套门禁全绿、运行时 0 错误。

- **v24 顶部类型切换升级为主分类器（用户：按顶部的类型分类）**：把顶部「中国占卜/国外塔罗」切换从「仅换肤」升级为「主类型分类器」——选中即同时切主题 + 按类型筛选内容标签。实现：① ui.html 删除冗余的左侧分类行(.cats：中国占卜/国外塔罗/工具)，顶部切换成为唯一分类入口；② app.js 抽全局 applyTheme(t)，新增 filterTabsByType(type)：TABS 按 data-cat 显隐(type 自身 + tool 常驻可见，其余隐藏)；顶部 button.onclick = applyTheme(t)+filterTabsByType(t)。③ 初始化统一：主题按 xuanji_theme_v2 还原，内容按 nav.cat(cn/tarot/tool) 还原后调 filterTabsByType。④ 移除旧 showCat/CATS 左栏点击逻辑(CATS 现为空，forEach 安全 no-op)。工具类 settings/history 始终可见便于进入。专项验证：默认 20 标签(18中国+2工具)+宣纸；选国外塔罗→4 标签(tarot/star/settings/history)+body=theme-dark theme-tarot ✅；选中国占卜→20 标签+宣纸 ✅。构建 810KB，三套门禁全绿、运行时 0 错误。

- **v25 动效打磨 + 西式深化 + 测字模块 + 准确率分层（用户：优化视觉体验，然后再扩充内容以及准确率）**：① 动效：令牌时长放慢(--t-story 1.0s)、位移加大(--m-emph 26px)、揭幕 saga 2000→2600ms、逐笔运笔 .84→.98s、星图/八字专属星序延长；② 西式深化：塔罗新增关系阵(6位:我/对方/过去/现在/未来/建议)与决策阵(4位)、灵数新增流年数 + 姓名灵数(cnchar 笔画累减)真正参与计算、符文/雷诺曼加主题关键词锚；③ 中国占卜新增测字模块(确定性：cnchar 笔画→八十一数表→五行)；④ 准确率：CRED 新增 calc 层（推算·确定性算法，区别于 det 真实历法 / div 问签），BASIS 扩展 cezi/numerology 可核验链路；maybeFold 的 rmeta 按 pid 区分（div 不标 lunar.js、calc 标"非历法、非随机"）；八字四柱与 lunar.js 直算交叉一致(庚午 壬午 辛亥 甲午)。关键经验：wrapCompute 用 closest('.card.panel') 取面板（按钮基名≠面板 id，如 numoBtn→#numerology，getElementById 会漏打 data-read-modal）；jsdom 验证必须 require 绝对路径('C:/.../node_modules/jsdom')，NODE_PATH 裸名在该环境下解析失败。构建 975KB，三套门禁(verify_cascade/verify_motion/test_real) + 专项(verify_new/verify_acc) 全绿、运行时 0 错误。

- **修「切换的时候会跳动」（类型切换塌缩导致 stage 垂直跳动）**：根因：顶部类型切换 filterTabsByType 用 `display:none` 隐藏 `.subcats`(tool 时还隐藏 `#secIntro`)，其空间瞬间消失，使下方高度锁定的 `.stage`(height:min(86vh,960px)) 整体上下窜动=跳动；stage 自身高度锁定、tabs 单行横滚不换行，故面板内 cross-fade 不跳。修法：subcats/secIntro 隐藏由 `display:none` 改为 `.collapsed` class（`max-height:0;opacity:0;margin:0;overflow:hidden` + `transition:max-height/opacity/margin .34s`），元素保留文档流、高度平滑收放，stage 改为平滑滑动而非瞬跳。jsdom 探针确认切到国外塔罗时 subcats.display 仍为 flex 且带 .collapsed（不再 none）。三套门禁+专项全绿。

- **修「每个 tab 切换都会卡一下」（面板 cross-fade 二次播 panelIn 动画）**：根因：`.stage>.panel.show` 带 `animation:panelIn .42s both`(translateY(6px)+opacity 0→1)；switchPanel 的 cross-fade 分支用 inline `animation:none` 临时压制它，但 cleanup(300ms) 把 `p.style.animation=''` 清掉 → CSS panelIn 立刻重新激活，切换完成后再播一次 .42s 入场动画=肉眼「卡一下/顿一下」。次要源：`void p.offsetWidth` 在重内容面板(星图/八字 canvas、SVG)上同步强制重排。修法：① CSS 新增 `.stage>.panel.show.xfade{animation:none}`，cross-fade 入口给 p 加 `xfade` class（永久保留，不随 cleanup 移除）；首屏/reduced 分支仍无 xfade、播一次 panelIn 优雅入场；② 把 `void p.offsetWidth` 启动 opacity 过渡改为双 `requestAnimationFrame`，避免对重内容面板同步阻塞重排。jsdom 探针确认：首屏 bazi 无 xfade(播一次入场 OK)、切换后 name/切回 bazi 均带 xfade=true、.xfade 抑制规则存在。构建 976KB，三套门禁+专项全绿、运行时 0 错误。

- **需求 B：研读八字/占卜古籍 + 深化八字与占卜模块（用户：去网上读取八字/占卜相关书籍，继续优化扩充）**：① 研读：5 次 WebSearch 覆盖八字(渊海子平/三命通会/子平真诠/滴天髓/穷通宝鉴)、梅花易数、六爻(京房纳甲/增删卜易)、测字秘牒(程省十法/心易六法)、奇门(遁甲演义)核心理念。② 八字(baziRead+baziBtn)新增「格局识别(子平格局法·月令定格)」+「十神组合诀(食神制杀/官印相生/财星破印/比劫争财/伤官见官等)」：取月支本气十神定格局(GE_JU 十格表)、透干判定清纯度、十神组合检测九类；沿用 _hashStr 变体去模板。③ 六爻新增京房 SHI_BY_NAME(64卦世位表)→世应生克(世生应/应生世/世克应/应克世/比和)+持世诀(父母/子孙/官鬼/妻财/兄弟持世)+六亲发动诀(父动克子孙等)。④ 梅花重构体用生克为分级 tyRel，新增体党用党、卦气旺衰(月令五行生体用)、各卦事象(用卦五行象)、先吉后凶/先凶后吉(本卦vs变卦体用分级)。⑤ 奇门新增八神含义(SHEN_MEAN)、门迫/宫克(八门五行克九宫五行)。⑥ 测字新增会意谐音断辞(cnchar 拼音去调匹配 CEZI_HOMO)+五行取格法(CEZI_GE 生发/显达/厚载/决断/智流等)。验证：10 样本八字探针确认格局随月令差异化(建禄/正财/正官/七杀)、十神组合生效；测字/梅花/奇门/六爻专项探针全段命中、运行时 0 错误；test_real + verify_cascade/motion/new/acc 全绿(verify_acc 唯一❌为手写样例值，lunar.js 直算交叉校验✅一致)。构建 842KB。关键经验：jsdom 设 <select> 须用 selectedIndex 而非 value=String(n)（后者在 jsdom 下偶发回读为空导致 parse 失败）；六爻世位用京房八宫表最稳，勿靠位运算推。

- **继续需求B：梅花/奇门补「问事场景」聚合判词（用户：继续）**：在 ui.html 梅花/奇门面板各加 `mhScene`/`qmScene` 下拉(通用/事业/求财/感情/出行/健康/学业/诉讼/寻物/决策，默认通用不追加)；app.js 新增 `meihuaSceneLine`(基于体用关系 rB.rel 取向 + 体党用党 dangTxt + 先吉后凶 xj，按场景用神名 USE 生成"与身相生/相克/比和——宜X"聚合句)与 `qimenSceneLine`(基于值符宫门 zfMen 吉凶 menG + 门迫 poStr 场景化 PO + 八神/驿马/空亡场景特判)。两段均以真实变量驱动、去模板化，默认场景空则返''不增扰。验证：梅花4场景(事业/求财/感情/出行)探针确认用神/体用取向/势/时机随卦象差异化；奇门5场景(求财/出行/寻物/感情/健康)确认门吉凶/门迫/驿马/天芮/六合特判命中；test_real + verify_cascade/motion/new/acc 全绿、运行时0错误。构建 846KB。

- **继续需求B：六爻补问事场景聚合判词（用户：继续优化）**：在 ui.html 六爻面板加 `lyScene` 下拉(同梅花/奇门选项);app.js 新增 `liuyaoSceneLine(scene, ctx)`,基于场景→用神六亲映射(事业/诉讼=官鬼、求财/寻物=妻财、出行/学业=父母、感情=妻财·官鬼、决策=世爻)+世应取向 shiYing+用神持世/发动旺衰(既持世又发动/持世/发动/安静四态)+非用神持世补充提示,变量驱动去模板;在 liuyaoBtn 读取 scene 并插入渲染(默认通用不追加)。验证:9 场景探针全命中、0 错误,聚合句随世应/持世/动爻真实变量差异化(求财"妻财既持世又发动→宜把握时机"、出行"父母持世→宜以静制动"、决策"世爻→宜权衡而定"并附兄弟持世提示)。test_real + verify_cascade/motion/new/acc 全绿。构建 848KB。占卜三类(梅花/奇门/六爻)至此均带问事场景聚合判词,能力一致闭环。

## 2026-08-12 续·八字补调候用神+纳音论命（文献研读落地）
- 研读来源：《穷通宝鉴》(调候为急：十日干×十二月令最需天干) + 《三命通会》(六十甲子纳音意象，李虚中古法以年柱纳音立命)。
- 八字模块新增两路判词（与既有「格局(子平)/十神组合/旺衰」三路互参）：
  - 【调候·穷通宝鉴】`TIAOHOU[日干][月支]` 查表得调候喜神（如甲生寅月需丙·癸），说明"调候管气候、格局管架构、旺衰管强弱"三路并行。
  - 【纳音·年柱象】`ny0`(年柱纳音)经 `nayinImg()` 取意象（如海中金=深藏不露）。
- 关键修复：`ec.getYearNaYin()` 在本环境返回空串 → 纳音段被 `if(ny0)` 守卫跳过、四柱展示纳音也空缺。自建 `NA_YIN_60`(干支→纳音名 全60甲子映射) 覆盖，ny 改为 `pillars.map(g=>NA_YIN_60[g]||'')`，同时修好四柱纳音展示。
- 验证：test_real 八字专项 RUNTIME_ERRS:0；调候/纳音段真实渲染、随日干·月令·年柱差异化；verify_cascade/motion/new/acc 全绿（无 ❌）；fortune.html 重建约 850KB。
- 工具备注：本会话 Bash/Edit/Read/TaskUpdate 间歇性报"必填参数未传入"(基础设施故障)，改用 PowerShell 通道执行 node 补丁脚本(_patch2/3/4)完成写入，Write/Grep/PowerShell 可用。

## 2026-08-12 续2·八字应期(刑冲合害)两处渲染 bug 修复
- 上轮落地的 `baziYingQi`(大运流年应期·刑冲合害)经 10 样本全文探针发现两类渲染 bug，已修：
  - **自刑 undefined**：原代码 `rel.push([z,'','自刑',z,''])` 把地支(如'辰')当宫位键查 `PAL`，得 `undefined` → 渲染成「undefined之宫主…」。修法：自刑改记柱索引 `rel.push([i,z,'自刑',z,''])`，渲染时由「该地支落在哪一柱」反查宫位名 `PAL['年月日时'[i]]`（多柱同支则「、」并列），并给自刑独立判词「自我纠结、内耗、反复，宜修心守静、勿钻牛角尖」。
  - **三合重复/同字误判**：原按"每个组成支各记一条"导致同局重复(辰子局成×2)；同支三重被误判成「三合子子子局成——水生水生水之势汇聚」。修法：删 `SANHEG`，改 `SANHE_QUAN={水:[申子辰],木:[亥卯未],火:[寅午戌],金:[巳酉丑]}`，仅当三不同支齐备(`dzSet=去重后`)才算成局、按局五行论，去重。
  - 验证：①逐段探针 `window.__SEG` 暴露各 wrapper 段，定位 undefined 落在 `readHtml`(白话解读)内的应期段；②`_he3` 直接调 `baziYingQi` 构造地支，确认四局正确渲染、同支三重/半合正确省略、重复支去重、无 undefined；③10 样本全文 UNDEF=N、RUNTIME_ERRS=0；④verify_cascade/motion/new/acc + test_real 五件套门禁无 ❌。构建 852KB。
- 可复用工作法（fortune.html 持续打磨必用）：①app.js 为 CRLF，node 补丁须先 `s.split('\r\n').join('\n')` normalize，否则多行锚点 MISS 致脚本提前退出；②Bash 通道间歇故障 → PowerShell 绝对 node 路径执行脚本；③PowerShell 不回显 stdout、Grep 读不出中文文件 → 探针输出纯 ASCII 标记(RUNTIME_ERRS:/UNDEF=)经 `Out-File -Encoding ascii` 落日志再用 Grep 核验；④jsdom 隔离 undefined 用临时 `window.__SEG` 暴露各段。临时探针/日志清理时 Bash `rm` 被沙箱静默拦截，须用 PowerShell `Get-ChildItem|Where{$_-like"_*"}|ForEach{Remove-Item -Force -ErrorAction SilentlyContinue}` 循环多次删净。

