# 2026-08-19 工作日志

## 玄机阁占卜 Web 应用 — 历史遗留方向推进 + 塔罗展示优化

### 本轮三项交付（task #291 / #292 / #293，全部完成并部署）
线上：https://0279ad797a3b478488df88c02cee5abe.gz4.agentos-app.net
构建 md5：495c792562b3eb3cc3c8b99b8924fb9e（四端一致）

**#291 生命灵数 tab 扩充**
- 毕达哥拉斯体系补全：天赋数字(giftedNums)、人生四大高峰/挑战周期(lifeCycles)、灵数合盘兼容度环形图(numerCompat)。
- UI 新增「伴侣/合作者生日」输入 id=`numoPair`（注意：JS 读 numoPair，非 numoPartner，曾因测试用错 id 误判未生效）。
- 验证：浏览器测算 1995-07-21 + 合盘 1993-03-09，输出含 天赋/高峰/挑战/灵数合盘/兼容度 全 true。

**#292 塔罗叠牌位置进阶读法**
- 新增 overlayRead(picks,posN)：跨位结构洞察（同花色串联 + 大阿卡那跨层呼应，优先全保留）+ 相邻张力（四元素相济/冲撞/中性）+ 数字趋势。
- 修复：原 _ELEM_SUP/_ELEM_GEN 混用五行与四元素且有重复键 bug → 改为干净四元素 _ELEM_FRIEND/_ELEM_TENSE。结构洞察优先（slice(0,5) 不被裁掉）。
- 接入 runSpread 结果串。验证：vm 探针 + 浏览器均通过。

**#293 塔罗牌展示效果优化（含一个真 bug 修复）**
- 修复逆位双重旋转 bug：tarotCardSVG 已内部 rotate(180 100 145)，旧 setCard 又 rotate(180) 容器 → 逆位牌画面正立但文字倒置。改为容器不再旋转，加 `.tcard.rev::before` 正立「逆」徽标。
- 新增：3D 鼠标跟随倾斜(cardTilt/cardUntilt，rotateX/Y+translateZ+scale，mouseleave 复位)、金光扫过(.tc-sheen/.fc-sheen)、元素光晕(.tcard.elem-fire/water/wind/earth/major 按花色上色)、洗牌牌背金色几何 SVG 纹样。
- 单抽扇形(fan-card.picked) 同样升级；reveal 动画结束后移除 .reveal 类（animationend 监听）以释放 tilt 内联 transform。
- 验证：浏览器实测 w=128、tiltOn=rotateX(3.17)…、逆位容器 transform 空 + svgRot 正确、sheen 有、无运行时错误。

### 技术要点（可复用）
- vm 探针通用 shim：用 `new Proxy(function(){return U;}, handler)` 吸收全部 DOM/canvas 调用，handler.get 对 Symbol.toPrimitive 返回 ()=>0 避免 ToPrimitive 抛错；window.matchMedia 早返回以跳过 wireAmbient canvas 初始化。
- 构建：node build.js → cp fortune.html/index.html → deploy/ + wechat-miniprogram/ 四端 md5 同步。
- 验证链：node --check → vm 探针 → build → 四端 md5 → agent-browser 真浏览器 eval(getBoundingClientRect/dispatchEvent) → 部署 → curl grep 线上标记。
- agent-browser 无读图能力：用 eval + getBBox/getBoundingClientRect/dispatchEvent 做几何与交互验证替代肉眼。

### 目录迁移（重要）
- 2026-08-19 02:57 应要求将整个工作区从 `C:\Users\42134\WorkBuddy\2026-08-05-00-54-43` 移至 **`D:\WorkBuddy\2026-08-05-00-54-43`**（robocopy /MOVE，58 文件 11.69MB，0 失败，md5 校验由 robocopy COPY 完成）。
- C 盘仅残留空目录壳（当前会话进程占用，重启后手动删）。
- 已核验全部 automations 的 cwds，无一引用旧路径，无断链。
- **后续所有路径引用一律使用 D:\WorkBuddy\2026-08-05-00-54-43**（memory、build、deploy、agent-browser 验证均基于新路径）。
