# 2026-08-18 工作日志

## 五行穿衣「贴合」优化（user「继续优化五行穿衣，贴合一点」）
- 背景：五行穿衣模块已有每日五档（大吉/次吉/平/消耗/不利）+ 本命参照，但需手动点按钮、手动填生日，且不贴合当下时辰与当年流年。
- 优化点（app.js，ui.html 仅微调生日 label 文案）：
  - 免点按钮：`switchPanel` 切到 dress 面板时自动调用 dressBtn.onclick()（p.id==='dress' 分支）。
  - 自动接本命：`window.__baziYong` 已算过八字盘则自动联动，本命参照块标注来源「已自动接『八字命盘』喜用」/「生日私配」。
  - 此刻·时辰贴合：`ZHI[Math.floor((h+1)/2)%12]` 取当前时辰地支→五行→生我(GEN_OF)=此刻最旺色，实时渲染「此刻 HH:MM（X时·Y旺）→ 此刻最旺 Z色」块。
  - 今年·流年贴合：`yearGZ_LC(年)` 取当年干支→年干五行，对比本命喜用/忌神：喜用→打底色系；忌神→少作大面积；无冲→正常穿。
  - 今日直接照穿：外套(大吉·60%)/内搭(次吉·30%)/下装鞋(平·过渡)/配饰(消耗·10% 点缀) 具体部位公式块。
- 验证：probe_dress2.js 全过（PASS=24：时辰→地支→五行→最旺色 5 组、2026 丙午流年贴合 4 组、五档五行全排列+大吉=生我 10 组、色板完整 5 组）；node --check 通过；build 1048KB（app 456932 字节）；fortune.html grep 到 4 处新逻辑；cp -f 同步 deploy/+wechat-miniprogram/（4 文件均 1367494 字节）。
- 部署：CloudStudio 沙箱 0279ad797a3b478488df88c02cee5abe verified:true，分享链接 https://0279ad797a3b478488df88c02cee5abe.gz4.agentos-app.net。
- 结论：五行穿衣从「通用五档」升级为「流日五档 + 此刻时辰 + 当年流年 + 本命喜用」四维贴合，打开面板即出结果、零手动输入。

## 我的档案（个人模板 + localStorage 持久化）（user「生成一个个人的模板，然后把数据保存」）
- 需求：输入一次个人出生信息生成模板并保存，全模块自动带入，免重复填写。
- 实现（ui.html settings 面板顶部新增「我的档案」区块 + app.js 档案逻辑）：
  - 字段：出生日期(profBirth)/出生时辰(profHour，偶数中点格式 0,2,4…22)/性别(profGender)/出生地(profCity，东经，真太阳时)。
  - PROF_KEY='fortune_profile_v1'；profSave/profClear/profLoad 读写 localStorage。
  - profApply：回填八字(birth/hour/gender/baziCity)、五行穿衣(dressBirth/dressHour)、幸运色(luckyBirth/luckyHour)、紫微(zvBirth/zvGender/zvHour)、生命灵数(numoBirth)；自动算喜用（有时辰→baziStrength 真实旺衰，无时辰→扶抑近似）写入 window.__baziYong 等全局。
  - 时辰换算 profHourToBazi：中点小时(0,2,…22)→起点小时(0,1,3,…21)，h===0?0:h-1（八字/紫微用起点，穿衣/幸运色用中点）。
  - profRender 档案摘要：生日/时辰档/性别/生肖(Solar.getYearShengXiao)/日主喜用；保存后若 dress/lucky 面板可见自动联动刷新。
  - 页面加载自动 profApply() 恢复档案。
- 验证：probe_profile.js 全过（PASS=13：时辰换算 5、喜用计算 4（1990-06-15 丑时→日主辛金·喜用土金）、存储往返 2、生肖 2（1990/2026 均马））；node --check 通过；build 1055KB（app 461161 字节）；cp -f 同步 deploy/+wechat-miniprogram/（4 文件均 1374933 字节）。
- 部署：CloudStudio 沙箱 0279ad797a3b478488df88c02cee5abe verified:true，分享链接 https://0279ad797a3b478488df88c02cee5abe.gz4.agentos-app.net。
- 结论：个人模板一存，八字/五行穿衣/幸运色/紫微/灵数全站自动带入，刷新不丢，零重复输入；与「五行穿衣」形成「档案→喜用→穿搭」闭环。

## 档案驱动「今日运势聚合」个人判词（user「继续」衔接档案主题）
- 需求：把已保存的「我的档案」进一步灌入全站，让首页今日总览也按个人喜用给出判词，不只回填输入框。
- 实现（app.js todayBtn.onclick 内）：新增 `profileToday` 变量（有档案+window.__baziYong 时才渲染），插入今日运势聚合的「生肖」段前。判词对比「今日日干五行 wu vs 本命喜用 myYong」四档：
  - 喜用命中 → 旺你，多穿该系；
  - 生我者(由 SHENG[e]===myWu 反查 yangMe)含喜用首元素 → 喜用所生滋养今日；
  - 克我者(KE[e]===myWu 反查 keMe)含喜用首元素 → 喜用压阵镇场面；
  - 其余 → 无冲无合，常态发挥。
- 关键：SHENG/KE 方向是「我生/我克」表（SHENG[木]=火 木生火），反查 `SHENG[e]===myWu` 得「生我者」、`KE[e]===myWu` 得「克我者」，语义与文案已对齐（避免误导）。判词文案强调"今日 vs 我的喜用"关系而非简单五行罗列。
- 验证：node --check 通过；build 1056KB；纯 node 探针判词 5 分支全过（火vs[火]命中/火vs[木]生我用神/火vs[水]克我/火vs[金]无冲/木vs[木]命中）；cp -f 同步 deploy/+wechat-miniprogram/（4 文件均 1377013 字节）。
- 部署：CloudStudio 沙箱 0279ad797a3b478488df88c02cee5abe verified:true，分享链接 https://0279ad797a3b478488df88c02cee5abe.gz4.agentos-app.net。
- 结论：档案从「回填输入框」再进一步到「驱动每日吉凶判词」，让"我的档案"真正贯穿首页总览；与五行穿衣/幸运色形成「档案→ 每日五行→ 个人吉凶」闭环。

## 命理功能扩充收尾（user「添加小象传补全 每日一签/值日神煞 本命佛/守护佛五尊 六爻装卦完整排盘…」）
- 四项功能落地 + 一项解释性说明，全部完成并部署（fortune.html/index.html 均为 1412715 字节，md5=ff32bbd3，四端 deploy/+wechat-miniprogram/ 一致）。
  - ① 小象传补全：六十四卦象辞注解补齐（沿用周易本义）。
  - ② 每日一签：并入今日运势聚合（todayBtn，`dailyQian` 区块）。
  - ③ 值日神煞：并入黄历面板（建除十二客/吉神宜趋/凶煞宜忌）。
  - ④ 本命佛/守护佛五尊：并入「数字生肖」num 面板「生肖配对」下新增「本命佛 / 守护佛」sub-sec。BF_MAP 十二生肖→八大守护尊真实对应（鼠→千手观音、牛虎→虚空藏、兔→文殊、龙蛇→普贤、马→大势至、羊猴→大日如来、鸡→不动明王、狗猪→阿弥陀佛）；BF_FIVE 五方佛（中央毗卢遮那/东方阿閦/南方宝生/西方阿弥陀/北方不空成就）；五行方位归位（鼠猪→北水、虎兔→东木、马蛇→南火、猴鸡→西金、其余→中央空）；本命年联动：`Solar.getLunar().getYearInGanZhi().charAt(1)` 取流年太岁地支 vs 档案生肖，值太岁→红边提示。今日运势聚合自动带出「今日本命佛」IIFE 区块。
  - ⑤ 六爻装卦完整排盘确认：GONG_GAN（卦宫纳甲）/sixQin（六亲）/SHI_BY_NAME（世应）/六神/飞伏/旬空/墓库/三合/化进退/暗动全齐，排盘完整。
- 验证链：node --check app.js → build.js（保留 lunar+cnchar 内联库）→ DOM shim + vm 探针（BF_MAP=12/BF_FIVE=5/十二生肖全覆盖/本命年分支 isBN=`值太岁` vs isNBN=`并非你的本命年`）→ ui.html 元素 ID 一致性 → 四端同步 md5 一致 → 部署。
- 部署：CloudStudio 沙箱 0279ad797a3b478488df88c02cee5abe，分享链接 https://0279ad797a3b478488df88c02cee5abe.gz4.agentos-app.net。
- 关键经验（vm 探针）：`vm.runInContext` 顶层 `const/let` 不挂 global（只有 var/function 挂），访问 const 常量须用 `vm.runInContext('BF_MAP',global)` 在 vm 内部求值（inside() 辅助）；lunar UMD 在 vm 中不导出到 module.exports 而是挂全局，跨 vm 上下文访问 Solar 存在环境差异（probe 局限非 app bug，浏览器中 Solar 全局可用）。

## 本命行星相位评分模块（Task #286，user「按照你的来」实施西式星盘方向A）
- 需求：用户原问「生辰星盘相位图很高是什么意思」→ 判定西式完整星盘（宫位/相位环）复杂度高、收益/成本比低，用户授权「按照你的来」→ 落地「本命行星相位简化评分」并入现有星座面板 star，不做完整宫位盘（避免散件）。先给出结论再给方案，符合用户「显式判定+功能整合」偏好。
- 算法：Paul Schlyter 行星轨道根数近似计算八大行星地心黄经（N/i/w/a/e/M + 日月速率），开普勒方程迭代解近点角；太阳/月亮用简化天体公式。已用实际天文日期验证：1990-06-15 太阳 84.1°、2000-06-21 夏至 90.4°、2026-08-18 太阳 145.58°（狮子座25°58'，正确）。
- 相位/容许度：合0°±8、六合60°±6、刑90°±7、拱120°±8、冲180°±8；权重 合/刑/冲=3（结构张力）、六合/拱=2（顺畅助力）。`PH_RANK` 统计每星成相数+加权强度分，`topSc>=12→'高' / >=8→'中' / 其余→'稳'`。"相位图很高"= 该行星成相多且多为强相位→强度分高→本盘最亮。
- 落地文件：app.js 新增 PH_ZOD/PH_SIGN/PH_ORB/_phSUN/_phMOON/_phPLANET/PH_ORDER/PH_LONS/PH_ASP/PH_BETWEEN/PH_MEANING/PH_TONE/PH_RANK/PH_TONE_JOIN/PH_HTML（插在上升星座注释前）；starBtn.onclick 内用 gregorianToJD(y,m,day,h)+PH_LONS+PH_HTML 注入模板 ${triHtml} 之后。ui.html 零改动（复用 starBirth/starTime）。
- 精度诚实：PH_HTML 脚注注明「太阳/月亮简化式，八大行星 Paul Schlyter 轨道近似误差约1–2°，供趣味参考」。
- 验证链闭环：node --check app.js → probe_phase_vm.js（vm+DOM shim 加载 app.js，验证 PH_ORDER_LEN=10/PH_ORB_CNT=8/SUN_IN_LEO=PASS(120-150°)/HTML_HAS_BRIGHTEST+HTML_HAS_DETAIL=PASS）→ build.js 1083KB → probe6(jsdom 顶层零错误)/probe7(64卦齐)/verify_star → 四端同步 fortune.html=index.html=deploy/=wechat-miniprogram/（md5=0161b209 一致）→ 部署。
- DOM shim 关键补充（probe_phase_vm）：需 `documentElement.style.setProperty/removeProperty`、canvas `getContext('2d')` 返回完整 ctx 桩、`styleStub`；object 字面量花括号须严格配对（曾「Missing initializer」= doc 对象多闭合了一层 `}}},`）。定时器残留（sp.remove）用结尾 process.exit(0) 干净退出。
- 部署：CloudStudio 沙箱 0279ad797a3b478488df88c02cee5abe verified:true，分享链接 https://0279ad797a3b478488df88c02cee5abe.gz4.agentos-app.net。

## 移动端体验优化（user「优化移动端体验」，非扩功能、纯体验专项）
- 核心痛因：原应用是桌面优先设计，`.stage` 恒定 `height:min(86vh,960px)` + 内部滚动，switchPanel 用 absolute 交叉淡出切换。手机上=「页面滚动 + stage 内嵌滚动」双重滚动、长面板被 86vh 裁切、首屏 header 堆叠占屏高、触控目标偏小。
- 改动一（app.js switchPanel #90）：加 `const flat=(window.innerWidth||document.documentElement.clientWidth)<=768;`，移动端走 `if(reduce||flat||!cur){...inkSwirl...return}` 直接线性切换——不启用 absolute 覆盖，彻底消除嵌套滚动。桌面（>768px）保留原 cross-fade。
- 改动二（ui.html 新增 `@media(max-width:768px)` 移动专项块，插在 680 块后）：
  1) stage/panel 改自然流：`.stage{height:auto!important;min-height:0;overflow:visible!important}` + `.stage>.panel{...position:static!important}` + 隐藏面板滚动条——长面板随页面自然滚动不裁切。
  2) 首屏瘦身：seal 52→38px、h1 30→22px 字距 10→7、sub/todayline/wxchip(22→18px)/theme-switch/top-settings(40→34px) 整体压缩，让内容尽早见光。
  3) 吸顶 tabs：top:0、touch 目标 `.tab{padding:10px 13px}`。
  4) 触控高度：`.btn:not(.mini),button[type=submit]{min-height:46px}`（注意排除 .btn.mini 22 个 chip 小按钮）、input/select/textarea padding 加大、font-size:16px 防 iOS 缩放。
  5) 溢出清理：`html,body{overflow-x:hidden}body{-webkit-tap-highlight-color:transparent}img,svg{max-width:100%}`。
  6) 密度：.ne-grid 2列、.grid2 单列、.card padding 收窄。
- 验证：node --check app.js → build 1086KB → probe6(jsdom 顶层零错误) → probe_mobile.js 专项（jsdom 模拟 innerWidth=375：flatBranch=true、切换 name 面板 hasShown、panelPosition=relative 无 absolute 残留、errors=[]）→ probe_phase_vm（相位模块未破坏全 PASS）→ 四端同步 md5=8e0a9084 一致。
- 探针经验：jsdom 中 getComputedStyle 须用 win.getComputedStyle（Node 全局无）；innerWidth 用 Object.defineProperty 模拟。
- 部署：CloudStudio 部署 3 次 fetch failed（疑似 Clash 7897 代理网络抖动），待网络恢复后重试 deploy 目录即可；产物已在本地完成，四端同步 md5=8e0a9084 保持一致。

## 继续填充国外塔罗 tab 内容（user「继续填充国外塔罗里面tab的内容」）
- 现状评估：t-cat tarot 下 5 个 tab——塔罗(78张×正逆+8阵法+搜索)、星座(三要素+相位)已极满；生命灵数()较满；真正薄弱的是「北欧符文 runes(24符·仅单/三符阵)」和「雷诺曼 lenormand(36卡·仅单/三卡阵)」——只有孤立牌义，不真正回应"所问之事"。
- 填充策略（对齐塔罗丰富度、整合不散件、专业零凑数）：用「牌义关键词 × 问事透镜(love/career/wealth/health/all)」的组合式定向解读，而非 240 句硬写。
- 符文 runes 增强：新增 RUNE_LENS(问事透镜)、runeQRead(定向组合解读)、runeTrend(综断:按正逆比例+主导主题词)、runeResultHTML(牌位+定向落点+整体趋势版式)；重写 runeBtn/runeSpreadBtn 走新版式；新增五符十字阵 runeCrossBtn(现状/阻力/助力/未来/结果，即凯尔特十字简版)。
- 雷诺曼 lenormand 增强：新增 LENO_TONE(36卡吉中凶±=表)、LENO_LENS、lenoQRead(按倾向±=×关键词给定向批语，凶卡用 --cinnabar 警示色)、lenoTrend(按吉中凶计数综断)、lenoResultHTML；重写 lenoBtn/lenoSpreadBtn；新增七卡阵 lenoSevenBtn(我/对方/关系本质/阻力/助力/未知/趋势)。
- ui.html：runes 加「五符阵」按钮、lenormand 加「七卡阵」按钮（皆 tarot-btns 内 .btn.mini）。
- 坑：嵌套三元表达式缺括号 `(tone==='+'?'宜取势':tone==='-'?'宜避其害':'宜观势')` 报 Unexpected identifier → 补外层括号 `(tone==='+'?'宜取势':(tone==='-'?'宜避其害':'宜观势'))`。--cinnabar-light 变量不存在 → 用 --cinnabar 兜底。
- 探针 probe_runes.js：为正确验证按钮触发渲染，把 doc.getElementById 改为「按 id 缓存同一元素」(app.js 赋值与探针读取共享同一对象)，原始 el0() 每次新建对象导致 onclick 永远为 null。17 项全 PASS：RUNES_LEN=24/LENO_LEN=36/新按钮挂接/定向解读(感情·心之所向)/吉凶批语(利好·示警)/综断/版式含整体趋势/四种按钮渲染(含正则匹配多牌位)。
- 验证链：node --check app.js → build 1091KB → probe6(jsdom顶层零错误) → probe_phase_vm(相位回归全PASS) → probe_runes(17项全PASS) → 四端同步 md5=4dcb78d2 一致 → 部署。

== 卦象的图（task #290）+ 塔罗图案（task #289，2026-08-18）==
- 卦象的图：#290 完成——周易/六爻卦象（六爻）绘制升级 + 周易面板新增八股卦象速览；起卦结果输出可见的卦象 SVG。
- 塔罗图案：#289 完成——重写 app.js `majorArt(num)`（原极简 stylized → 22 张韦特 RWS 场景化构图），每张大阿卡那独立场景：0 愚者崖边旭日·1 魔术师四元素·2 女祭司双柱月冠·3 皇后麦穗·4 皇帝王座·5 恋人天使·6 战车双斯芬克斯·7 力量抚狮∞·8 隐士提灯·9 命运之轮·10 正义·11 倒吊人 rotate(180)·12 死神白马黑旗·13 节制天使两杯·14 恶魔倒五芒·15 高塔雷击·16 星星八星·17 月亮双塔犬·18 太阳孩童白马·19 审判号角·20 世界花环舞者。
- 调色板：G=#e9c479金 R=#c24234红 B=#7aa0d8蓝 W=#f6efdc白 D=#cfcfc2灰 Gg=#8fa884绿 T=#c9975e棕 E=#b39ac9紫 S=#6e5b8a 肤 sk=#e8c49b/sk2=#e0b288。辅助 draw fn：sun/moon/star5/star4/cloud/mtn/pillar/fig/robe/headOnly/dog/lionHead/sphinx/wing。
- 渲染入口：majorArt(0..20) 每张返回 960-3028 长度、12-35 元素；tarotCardSVG(name,rev) 端到端（含 grad id 用牌名唯一 uid）；调用点 setCard(2213/2242)、heroCard(7010)、单张放大(2398)。
- 坑：写入时残留错误——`fig` 辅助函数残留任意参数判定（简化为三参 fig(cx,torsoY,skin)）、命运之轮 `${sphinx(100,242,...[])}` 无效展开参数、星星卡 `<path d="M1" y2=""/>` 无效属性、月亮卡字符串内字面 `+`。全部修复。
- 探针：probe_tarotart.js（vm 需 docEl.appendChild:noop + hxGold 块；断言 ALL22_ART_OK/PASS 每张 len>100 元素≥6、DISTINCT_LEN=21 独立构图、CARD_NEST_BAD=0）、probe_tarotgeo.js（GEOMETRY_CLEAN=PASS 无 NaN/undefined/空路径）。make_tarot_preview.js 生成 tarot_preview.html(73KB) dark 背景 #0a1c22 grid 展示 22 张+关键字。
- 验证链：node --check app.js → probe_tarotart/probe_tarotgeo→ build → 四端同步 md5=b0772354154d09054e76d2b75ba77b78 → agent-browser 真浏览器：22 张大阿卡那在页内 len960-3028/元素12-35 全渲染、三牌阵实测出牌（圣杯骑士/权杖侍从/权杖10）+ 愚者卡含旭日光线场景、errors 空 → 部署线上 https://0279ad797a3b478488df88c02cee5abe.gz4.agentos-app.net 验证卦象速览+中外塔罗+预览页 200。
- 提醒：当前模型 deepseek-v4-flash 不支持读图，无法直接看 screenshot，改用 agent-browser eval + getBBox() 几何验证替代肉眼。
